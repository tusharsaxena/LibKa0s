# 02 — Deviations

**Standard:** v2.21.0 (2026-08-04). **Prefix:** `LK-` (assigned this run; stable thereafter).
**Scope note:** this repo is the shared **library**. Addon-only rules are recorded as N/A in
`01_CURRENT_STATE.md` with reasons and are **not** listed here. Nothing below is a rule that was
never meant for a library.

**Counts — MUST failures: 10, SHOULD failures: 5.** No deviation is Critical or High risk to a
shipped consumer; every one is a documentation, release-process or test-integrity gap.

| ID | Section | Severity | Deviation | Fix direction |
|---|---|---|---|---|
| **LK-01** | `documentation-§2` | **MUST** | No root `CLAUDE.md` stub. The repo has no agent-facing entry document at all, so a session opening it is told nothing about the standard, the green gate, or where the docs are. | Add a root `CLAUDE.md` stub: H1, adherence line, `## Standards compliance (read first)` verbatim in substance, a read-the-docs pointer list, and the green-gate line. Adapt "addon" to "library"; do **not** turn it into a full brief (#26). |
| **LK-02** | `documentation-§6` (anti-pattern #34) | **MUST** | The standards reference appears in **zero** durable places. Place 1 (TOC `## X-Standard:`) is N/A — no TOC. Place 2 (README badge/link) is absent — `README.md` has no badge row and never names the standard. Place 3 (`CLAUDE.md`) is absent (LK-01). The only mention anywhere outside frozen history is an incidental link in `docs/automated-tests/README.md:4`. | Restore both available places: a linked standard badge (or, if no badge row is wanted on a library README, an explicit "built to the Ka0s WoW Addon Standard" line with the repo URL) near the top of `README.md`, and the `## Standards compliance (read first)` section in the new `CLAUDE.md`. Record in `CLAUDE.md` that place 1 is structurally N/A here. |
| **LK-03** | `documentation-§7` | **MUST** | No root `DEPENDENCIES.md`. The toolchain this repo actually needs — `lua5.1` (hard: the kit uses `setfenv`), `luacheck` (via **luarocks**, not pipx), `lizard` (via **pipx**, not pip), `git`, POSIX `ls`, bash — is discoverable only by running something and reading the error. This repo is also where the wrong `pipx install luacheck` hint was found and fixed (`CHANGELOG.md` Unreleased), which is exactly the failure the section exists to prevent. | Add a root `DEPENDENCIES.md` with the three groups (Runtime / Development / Release-assets), evidence-based entries citing `file:line`, verbatim WSL2/Ubuntu install commands with the PEP-668 workaround, a one-line verify command per tool, versions where they matter, and the verified command set. Runtime section's honest content is "none — this library is vendored by copy". |
| **LK-04** | `documentation-§3` | **MUST** | `docs/testing.md` absent. The verify-how-to (commands, green gate, local toolchain, pointers to `test-cases.md`) lives in `README.md:122-159` instead. | Create `docs/testing.md` as the canonical verify page and reduce the README's `## Development` to a pointer, or keep a short README section and make `docs/testing.md` the full page. Must cover: `lua tests/run.lua`, `luacheck .`, the runner's `--suite lint --suite tests --no-bundle` fast path, Lua 5.1, the `--list` regeneration, and the `test_kitsync`/`test_versioning` gates. |
| **LK-05** | `documentation-§3` | **MUST** | `docs/ARCHITECTURE.md` absent. `README.md:196-230` gives a **file tree**; there is no engineer-facing module map with the seams, the inter-module floors, the failure modes and the known limitations. `docs/api/` covers per-version contracts, not the shape of the whole. | Create `docs/ARCHITECTURE.md` with library-adapted sections: Overview, Module Map (five majors / eight files / who depends on whom and at what floor), the descriptor surfaces, the `rawget` `L` seam, the record schema pointer, Taint Notes (the `Settings`/`SettingsPanel` reaches and their guards), Known Limitations. Mark Settings Schema / Message Bus / Event Subscriptions N/A with reasons. |
| **LK-06** | `localization-§5` | **MUST** | British spellings throughout authored text. Counts by area: `README.md` 4, `CHANGELOG.md` 36, `LibKa0s/*.lua` 31, `testkit/*.lua` 4, `tests/*.lua` 79, `docs/api/` 69, `docs/releasing.md` 7, `docs/automated-tests/` 24, `docs/adoption-prompt.md` 14, `docs/adoption-report.md` 3. Mitigation: **every** hit inside `LibKa0s/*.lua` is in a **comment** — no identifier and no shipped string is affected, so no host-visible symbol changes. | Sweep `colour→color`, `grey→gray`, `behaviour→behavior`, `synthesise*→synthesize*`, `normalis*→normaliz*`, `licence→license` across authored text. Exclusions that must be preserved: frozen history (`docs/adoption/`, `docs/reviews/`, `docs/superpowers/`), superseded `docs/api/` documents (never edited after they stop being current — `docs/releasing.md:53-55`), and quoted external text. `docs/test-cases.md` follows its suite names automatically on regeneration. |
| **LK-07** | `automated-tests-§6` | **MUST** | The release order does not produce the four-suite bundle. `docs/releasing.md:15-81` has nine steps ending in tag + re-vendor; none of them runs `tests/_kit/run-automated-tests.sh`. The one bundle that exists is not a release artifact: `manifest.json` reads `"release": null`, `"git": { "sha": "64c1b8a…", "dirty": true }` — a **dirty tree**, and a commit that is not the `v1.7.0` tag (`6ce8548…`). | Insert a numbered step into `docs/releasing.md` between the current 6 and 7: run the full bundle from a clean tree, with the release label, before the tag; the manifest then identifies itself as the release run. State that a dirty-tree bundle is not release evidence. |
| **LK-08** | `automated-tests-§3` (*The release gate*) | **MUST** | The commit-gate / release-gate distinction is stated **half**-correctly, which reads as a contradiction of the standard. `docs/automated-tests/README.md:29-36` tables `perf` and `complexity` as "no — recorded only" and says they are "**never used to fail a run**" with no release-checkpoint caveat; `RESULTS.md:9-11` says the same. Neither states that the **tag** is gated on all four suites plus `suites.complexity.warnings == 0`, that a `skip` blocks as NOT EVALUATED, or that this repo's standing `perf` skip (no `tests/perf.lua`) is the one narrow exception and **MUST be stated in the release notes**. `docs/releasing.md` states no release gate at all. | Add the release-gate paragraph to `docs/automated-tests/README.md` and to the release order in `docs/releasing.md`. Because `RESULTS.md` is **generated**, the corresponding sentence belongs in the emitter in `testkit/run-automated-tests.sh` (with a `Kit.VERSION` bump and a re-vendor), not in a hand edit of `RESULTS.md`. |
| **LK-09** | `testing-§9` | **MUST** | `tests/run.lua`'s two hand-maintained lists are ungated. `:17-19` names the eight library files with nothing comparing them to `LibKa0s/LibKa0s.xml:2-9`; `:76-80` names twelve suites with nothing comparing them to `tests/`. The `MAJORS` table at `:29-60` **is** gated (by `test_versioning.lua`), which makes the two ungated lists easy to mistake for gated. Measured by the 2026-08-05 review: a file added to the XML only, and a deliberately failing unlisted suite, each left the run at 480/480 green. Both are `testing-§9`'s two named silent failure modes, in the repo the section calls its own reference implementation. | Add a derivation suite: parse `LibKa0s/LibKa0s.xml` for `<Script file="…"/>` in order, publish the runner's actual load list through `Kit.expose`, and assert set + order equality and on-disk existence; separately assert that every `tests/test_*.lua` on disk appears in the suite list and vice versa. This is a `testkit`-adjacent capability — consider `Loader.xmlFiles()` beside `Loader.tocFiles()` so every consumer gets the same gate. |
| **LK-10** | `testing-§12` | **MUST** | `tests/test_perf_core.lua:15-23` asserts only **that** it raised, across four descriptor-validation cases — `testing-§12`'s explicit "**MUST NOT** treat *it raised* as sufficient". Measured by the review: no-op'ing `required(d, "name", "string")` at `LibKa0s/Perf.lua:290` leaves the suite 480/480 green, because `:328` indexes `d.name` and raises anyway. The case is therefore unfalsifiable against the guard it names. `Kit.assertError` exists (`testkit/framework.lua:64-71`) and the very next case (`:27-…`) already uses it correctly. | Convert the four `pcall`/`assertFalse` pairs to `T.assertError` with the library's own message text, so each case pins **which** guard fired. Add the `-- red under:` comment `testing-§12` recommends. |
| **LK-11** | `documentation-§5` | SHOULD | README `## Repo layout` (`README.md:213-225`) has drifted: it lists `docs/adoption/`, `docs/reviews/`, `docs/superpowers/`, `test-cases.md`, `releasing.md`, `record-schema.md` — and omits **`docs/automated-tests/`** entirely, the directory that now carries the trend line and every run bundle. It also predates `docs/audits/`. | Add `automated-tests/` and `audits/` rows to the layout block in the same sweep as LK-04/LK-05. |
| **LK-12** | `documentation-§3` | SHOULD | Three of the five required topic-detail docs are absent. `docs/perf-runs/README.md` — **accepted**: absent with a written reason (`docs/automated-tests/README.md:47-51` — a library cannot be loaded alone, so in-game captures live in the consumers' repos). `docs/smoke-tests.md` — absent with **no** written reason, same underlying cause. `docs/performance.md` — absent; the library's own performance page has no home, and the material is scattered across `docs/api/Perf/`, `docs/record-schema.md` and `RESULTS.md:44-56`. | Write the two absences down as decisions rather than gaps: a short `docs/smoke-tests.md` pointing at the consumers' suites and naming what only a live client can prove for this code, and either a `docs/performance.md` or an explicit N/A note in `CLAUDE.md`/`ARCHITECTURE.md`. Leave `perf-runs` as-is; the reason is already recorded. |
| **LK-13** | `performance-§9` | SHOULD | No `tests/perf.lua`, so `perf` is a permanent `skip` and `performance-§9`'s **zero-overhead scenario** — the evidence that a bracket is free when capture is off — does not exist for the library that supplies the brackets to eight addons. `RESULTS.md:44-56` states this plainly and refuses to let the green verdict imply otherwise, which is why this is SHOULD and not MUST. Compounding it, `LibKa0s/Perf.lua:377-378` claims a call site "pays one boolean test and nothing else" — the review measured two function calls plus the test, with nothing in the repo holding the number. | Add `tests/perf.lua` with at least the zero-overhead scenario (`P.Open`/`P.Close` with capture off vs. the path with instrumentation absent), asserting only deterministic quantities. Correct the `Perf.lua` comment to match what is measured — a comment change, not an API change (additive-only). |
| **LK-14** | `automated-tests-§1` | SHOULD | The single bundle was recorded from a **dirty working tree** (`manifest.json` `"dirty": true`), so its frozen numbers cannot be tied to any commit. A record whose provenance is "some uncommitted state on 2026-08-05" is weaker evidence than the bundle format is designed to carry. | Covered by LK-07's release step (clean tree before the run). No edit to the existing bundle — it is frozen; the next run supersedes it. |
| **LK-15** | `documentation-§5` / `testing-§10` | SHOULD | `docs/releasing.md:143-146` claims the API-document requirement is enforced "the same bargain `test_versioning.lua` strikes for the library's minors". It is not: `tests/test_kitsync.lua` **does** fail when the live kit revision has no API document, but `tests/test_versioning.lua:107-128` checks the **CHANGELOG only** — no major's API document is gated. Step 5's "a minor bump is not released until its document exists" is therefore remembered, not mechanical, for the five majors vendored into eight addons, while the single-consumer kit is gated. The priority is inverted. | Either correct the sentence, or — better — extend `test_versioning.lua` with the check `test_kitsync.lua` already performs: for every major, assert `docs/api/<Major>/version-<minors joined in load order>-docs.md` exists for the live `lib.MODULES`. That turns `docs/releasing.md`'s claim true rather than deleting it. |

## Explicitly checked and *not* recorded as deviations

- **anti-pattern #45 / #48** — `diff -r testkit/ tests/_kit/` is **empty** (run today). No `libs/`
  exists, so there is no vendored copy to drift; this repo is the upstream.
- **anti-pattern #47** — this repo *is* the library; there is nothing to hand-roll.
- **anti-pattern #51** — the committed complexity numbers **reproduce exactly** against a fresh
  verbatim `lizard` run today. No drift, no hand-editing.
- **anti-pattern #53** — the watch list carries two `Accepted` entries on the **first** recorded run.
  A three-run streak cannot exist yet; both entries carry real dispositions and one is explicitly
  flagged "the one to watch".
- **retired `docs/complexity.md`** — absent. Correctly retired, not carried as a second stale copy.
- **`automated-tests-§2`** — the runner is vendored, byte-identical, executable, and `.gitattributes`
  carries `*.sh text eol=lf`.
- **`testing-§5`** — `docs/test-cases.md` regenerates byte-identical today.
- **`layout-§1`'s 1500 cap** — met; both band files dispositioned.
- **`lint`** — 0/0, with the narrower-than-repo scope disclosed rather than hidden.
- **Review findings F-005 / F-006** (`P.Close(t0)` with no key raising on the on-path;
  `P.Cancel` leaving `P.context` set) — real defects, but they violate no MUST/SHOULD in the
  standard. They stay in `docs/reviews/2026-08-05/` where they were raised; this audit does not
  re-file them as deviations.
</content>
