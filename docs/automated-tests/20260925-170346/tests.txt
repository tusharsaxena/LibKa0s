  SKIP  suite inventory: tests/_kit/test_prose.lua is declined, and the decline is recorded — CLAUDE.md carries a `## Documented deviations` row keyed `localization-§5`: The kit ships a US-English gate (`tests/_kit/test_prose.lua`) and this repo leaves it unwired, running its own `tests/test_prose.lua` instead. `testing-§9` and `localization-§5` between them permit  ... — tests/test_prose.lua runs in its place
  PASS  core: IsConcatSafe is false for a table.concat-hostile value, true for a plain one
  PASS  core: SafeToString renders a secret as lib.SECRET and passes nil/booleans through
  PASS  core: MakeCloseButton draws the collection's own art when told who is asking
  PASS  core: no addon name is the multiplication sign, exactly as before
  PASS  core: an addon name the art cannot answer for falls back to the glyph
  PASS  core: the art is inset inside the click target, not filling it
  PASS  core: the icon reddens under the pointer and goes back
  PASS  core: Print joins with a space, prefixes verbatim, and routes through the injected sink
  PASS  core: sep separates the prefix from the body and may be empty
  PASS  core: a function prefix is re-read on every call
  PASS  core: a prefix that has not resolved yet prints the body alone
  PASS  core: Format applies the format string with pre-stringified args
  PASS  core: Format lands a secret in a numeric specifier as the format and its parts
  PASS  core: the default sink is DEFAULT_CHAT_FRAME:AddMessage
  PASS  core: :New refuses a descriptor with no prefix
  PASS  core: ApplySkin no-ops on a frame without SetBackdrop
  PASS  core: ApplySkin applies the skin table and both colors
  PASS  core: SKIN is the flat 1px Ka0s edge, not the 12px tooltip border
  PASS  core: ApplySkin synthesizes the inner highlight, exactly once
  PASS  core: ApplySkin survives a frame whose metatable answers every key
  PASS  core: ApplySkin tints a title and a divider when the frame carries them
  PASS  core: ApplySkin lays the backdrop down before anything drawn on top of it
  PASS  core: ApplySkin tolerates a frame with neither a title nor a divider
  PASS  core: ApplySkin honors an explicit skin table
  PASS  core: RGBA reads the keyed shape
  PASS  core: RGBA reads the positional shape
  PASS  core: RGBA lets the keyed shape win every channel, never mixing the two
  PASS  core: RGBA falls back per channel, so a three-element color keeps its default alpha
  PASS  core: RGBA keeps a stored false rather than swallowing it
  PASS  core: RGBA returns the defaults unchanged for a non-table
  PASS  core: RGBA does not default the defaults
  PASS  core: MakeCloseButton returns a button wired to onClick
  PASS  core: MakeCloseButton returns nil when CreateFrame is unavailable
  PASS  core: the player's class color is the client's, and nil for a class it cannot name
  PASS  core: the player's class color is memoized on SUCCESS only
  PASS  core: no unit but the player is ever cached
  PASS  core: an unresolvable unit answers nil rather than the player's color
  PASS  core: ResolveColor keeps the stored alpha under class color
  PASS  core: ResolveColor falls through to the stored rgb when the class does not resolve
  PASS  core: ResolveColor with the companion off never reaches the class palette
  PASS  core: ResolveColor answers four numbers for a swatch that was never stored
  PASS  core: SafeRegisterEvents registers around a retired name, front gate
  PASS  core: SafeRegisterEvent refuses a retired name for every AceEvent registrant, front gate
  PASS  core: SafeRegisterUnitEvent on a frame isolates a retired name, front gate
  PASS  core: SafeRegisterEvents registers around a retired name, probe frame
  PASS  core: SafeRegisterEvent refuses a retired name for every AceEvent registrant, probe frame
  PASS  core: SafeRegisterUnitEvent on a frame isolates a retired name, probe frame
  PASS  core: SafeRegisterEvent's front gate rejects without calling RegisterEvent
  PASS  core: SafeRegisterEvent on a raw frame, and with no rejected list at all
  PASS  core: SafeRegisterEvent on a Bus-stamped target is replayed after a stand-down
  PASS  core: Perf refuses to register when Core is missing or below NEEDS_CORE
  PASS  core: Perf's own stringifier renders a secret as <secret>
  PASS  env: GetAddOnMetadata reads the TOC through C_AddOns
  PASS  env: GetAddOnMetadata falls back to the deprecated bare global
  PASS  env: GetAddOnMetadata answers nil when neither reader exists
  PASS  env: Version answers the TOC version
  PASS  env: Version prefers the TOC over the fallback
  PASS  env: Version returns the fallback when the TOC cannot be read
  PASS  env: GetPlayerMapID asks C_Map for the player's map
  PASS  env: GetPlayerMapID answers nil without C_Map
  PASS  env: GetZone answers zone and subzone
  PASS  env: GetZone answers empty strings, never nil, when the readers are absent
  PASS  compat: registers minor 1 with exactly the nine public members
  PASS  compat: the Core floor keeps the major absent from a payload without Core
  PASS  compat: the source reads every global bare, never through the global table
  PASS  compat: IsSecret is false for every value when the client has no secrets system
  PASS  compat: IsSecret asks the client's own test
  PASS  compat: IsSecret normalizes a truthy non-boolean answer to true
  PASS  compat: CanAccess asks canaccessvalue where it exists
  PASS  compat: CanAccess without canaccessvalue is 'accessible unless secret'
  PASS  compat: CanAccess with neither global answers true, nil included
  PASS  compat: a secret that is accessible is still not a safe key
  PASS  compat: IsSafeKey(nil) is false with and without the secrets system
  PASS  compat: IsSafeKey is true for plain values, and v ~= nil with no system
  PASS  compat: Core's concat probe answers a different question and is not duplicated
  PASS  compat: GetSpellInfo flattens C_Spell's table into exactly six values
  PASS  compat: GetSpellInfo's modern rung is authoritative when it answers nil
  PASS  compat: GetSpellInfo remaps the legacy global's real shape, dropping the rank
  PASS  compat: GetSpellInfo answers a single nil when neither rung exists
  PASS  compat: GetSpellInfo takes a number or a string and calls no rung for anything else
  PASS  compat: GetSpellInfo passes a secret field through untouched
  PASS  compat: GetSpellInfo answers a single nil when the legacy global has no name
  PASS  compat: GetSpellName answers from C_Spell.GetSpellName first
  PASS  compat: GetSpellName falls to C_Spell.GetSpellInfo's name when the top rung is nil
  PASS  compat: GetSpellName treats a plain empty string as no answer
  PASS  compat: GetSpellName reads C_Spell.GetSpellInfo when C_Spell.GetSpellName is absent
  PASS  compat: GetSpellName on the legacy global returns its first value only
  PASS  compat: GetSpellName answers nil when no rung exists
  PASS  compat: GetSpellName returns a secret untouched and asks IsSecret before anything else
  PASS  compat: GetSpellName(nil) answers nil and calls no rung
  PASS  compat: GetSpellTexture returns one value, dropping the client's original icon
  PASS  compat: GetSpellTexture's modern rung is authoritative when it answers nil
  PASS  compat: GetSpellTexture falls back to the legacy global, one value
  PASS  compat: GetSpellTexture answers nil with no rung, and for a nil id calls none
  PASS  compat: GetSpellCooldown returns the modern table's five values in order
  PASS  compat: GetSpellCooldown reads a missing isEnabled as true and only true as active
  PASS  compat: GetSpellCooldown answers the inert tuple for a nil info table
  PASS  compat: GetSpellCooldown passes secret timings through without touching them
  PASS  compat: GetSpellCooldown derives isActive on the legacy global
  PASS  compat: GetSpellCooldown reads a zero legacy duration as not active
  PASS  compat: GetSpellCooldown reads a legacy isEnabled of 0 or false as disabled
  PASS  compat: GetSpellCooldown never orders a secret legacy duration
  PASS  compat: GetSpellCooldown is inert with no rung and calls none for a bad id
  PASS  compat: GetSpecialization prefers the namespaced reader, even its nil, one value
  PASS  compat: GetSpecialization falls back to the global, one value
  PASS  compat: GetSpecialization answers nil when neither reader exists
  PASS  compat: GetSpecializationInfo passes the namespaced multi-return through exactly
  PASS  compat: GetSpecializationInfo uses the global only when the namespaced reader is absent
  PASS  compat: GetSpecializationInfo(nil) calls no reader, and nil when none exists
  PASS  compat: with every rung removed each member answers the documented absent value
  PASS  compat: a host patching the shared table does not change another member's answer
  PASS  lifecycle: the major is registered and floors on Core
  PASS  lifecycle: the reserved hold keys are exported rather than spelled per host
  PASS  lifecycle: New refuses a descriptor missing any required field
  PASS  lifecycle: a fresh latch is up, holds nothing, and has fired nothing
  PASS  lifecycle: the first hold stands down, and the callbacks take no arguments
  PASS  lifecycle: holding a key already held is a no-op and does NOT stand down again
  PASS  lifecycle: releasing a key that is not held is a no-op and does NOT stand up
  PASS  lifecycle: Hold(a); Hold(b); Release(a) stands down ONCE and never stands up
  PASS  lifecycle: Hold; Release; Hold fires down, up, down — in that order, once each
  PASS  lifecycle: hold order is irrelevant to the edge count, in both directions
  PASS  lifecycle: the perf hold released under a live disabled hold leaves the addon down
  PASS  lifecycle: Set(key, truthy) holds and Set(key, falsy) releases
  PASS  lifecycle: Set is idempotent in both directions
  PASS  lifecycle: Reevaluate fires nothing when the set has not crossed
  PASS  lifecycle: Holds answers a FRESH sorted array, not the internal table
  PASS  lifecycle: the library prints nothing unless a line is asked for
  PASS  lifecycle: PrintHolds with no host printer answers false rather than raising
  PASS  lifecycle: a raising standUp still leaves the hold set empty and the latch up
  PASS  lifecycle: a raising standDown leaves the hold taken, so the release path still works
  PASS  lifecycle: a standDown that releases a hold fires standUp nested, and the latch ends up
  PASS  lifecycle: Hold and Release refuse a key that is not a non-empty string
  PASS  lifecycle: two latches share nothing
  PASS  bus: the major is registered and reports its file minor
  PASS  bus: the major is absent without Core, and with a Core below its floor
  PASS  bus: New refuses a descriptor without name, and a non-function isDown
  PASS  bus: the documented degradation stub matches the live surface
  PASS  bus: NewTarget answers a fresh target per call, and two receivers both hear one message
  PASS  bus: SendMessage on a tracked target is left raw, and reaches others while its own are down
  PASS  bus: the record follows every wrapper, and UnregisterAllEvents leaves messages alone
  PASS  bus: StandDown takes events AND messages down and answers the entry count
  PASS  bus: StandDown keeps the record and StandUp replays it
  PASS  bus: StandUp replays the record as it is NOW, not a snapshot taken at StandDown
  PASS  bus: a registration made while down is recorded and NOT live until StandUp
  PASS  bus: StandDown and StandUp are idempotent
  PASS  bus: re-registering a key replaces the entry in place and never duplicates it
  PASS  bus: every handler form survives the round trip with CallbackHandler's arguments
  PASS  bus: a raw register that raises while up reaches the caller and is not recorded
  PASS  bus: one entry that raises on replay does not stop the others, and StandUp never raises
  PASS  bus: a rejected entry is dropped from the record
  PASS  bus: StandUp is refused while isDown answers true, and the bus stays down
  PASS  bus: composed with Lifecycle, releasing one hold under another leaves the bus down
  PASS  bus: a re-embedded target is re-stamped at the next edge, so later registrations stand down
  PASS  bus: after a re-stamp, a registration made while down is recorded and not live
  PASS  bus: the re-stamp adopts the new raw member and counts a target once
  PASS  bus: a target only CallbackHandler holds survives StandDown and a full GC
  PASS  bus: a target emptied by its owner leaves the bus, which then holds nothing of it
  PASS  bus: two buses share nothing
  PASS  bus: the bus prints nothing across a full cycle
  PASS  bus: without AceEvent-3.0, NewTarget answers nil and the rest answer zero
  PASS  bus: Catalog accepts a conforming table and answers a fresh copy of it
  PASS  bus: Catalog refuses each malformed declaration, naming what is wrong
  PASS  bus: the catalog is strict on read and on write
  PASS  schema: the major is registered and reports its own live version
  PASS  schema: the module refuses to register without Core, and registers with it
  PASS  schema: the lib-level surface is exactly the six pure members
  PASS  schema: the instance surface is exactly the documented member list
  PASS  schema: the reference stub carries the whole surface, lib level and instance
  PASS  schema: the reference stub reaches neither the library nor the suite
  PASS  schema: the stub completes writes, in the live seam's order and to the same store
  PASS  schema: the stub's SetMany is all or nothing, and lands what the live batch lands
  PASS  schema: the stub writes a writeThrough path through, as the live seam does
  PASS  schema: the stub's Reset All sweep resets rows and keeps the sweep veto
  PASS  schema: the stub's lib level answers as the library's does on the same inputs
  PASS  schema: L overrides a STRINGS key, and a synthesizing L does not mask the rest
  PASS  schema: SplitPath memoizes by identity, drops empty segments, tostrings a number
  PASS  schema: Read walks a nested path and answers nil at every absence
  PASS  schema: Write creates intermediates and repairs a non-table one
  PASS  schema: SameValue compares scalars, -0, nested tables, both directions
  PASS  schema: a warm Read allocates nothing
  PASS  schema: New refuses a descriptor without rows and holds rows by reference
  PASS  schema: FindRow is first-wins, skips path-less rows, and refuses a non-string
  PASS  schema: AddRows appends, inserts at the head in order, and clamps past the end
  PASS  schema: a head insert re-indexes, so a new duplicate becomes the first
  PASS  schema: a host's in-place removal is seen after Reindex and not before
  PASS  schema: an unknown path is refused and nothing is stored or called
  PASS  schema: a write logs, then reacts, then announces, once each, and copies a table
  PASS  schema: a validate refusal names the path and carries why, and nothing happens
  PASS  schema: a bare-false validate is refused with why = nil
  PASS  schema: an instance-rooted write reaches the named instance, not the active one
  PASS  schema: an absent instance is refused with the resolver's reason, after validate
  PASS  schema: a resolver answering nil with no reason is refused as NO_ROOT
  PASS  schema: a row with its own set stores through it and never resolves a root
  PASS  schema: a sessionOnly row without set stores nothing and still reacts and announces
  PASS  schema: the inverted minimap row stores the inverse and reads back the value
  PASS  schema: debugEnabled false means neither debug nor format runs
  PASS  schema: the Set line carries format(row, v) when given and the value otherwise
  PASS  schema: with no debug, announce or format the seam still stores and reacts
  PASS  schema: a raising onChange propagates after the store and the line, before announce
  PASS  schema: Set resolves before it validates, and validate and the tail see the resolved id
  PASS  schema: a resolver naming no id leaves the caller's, and a false id is still an id
  PASS  schema: a closure or sessionOnly row hands validate and the tail the caller's id as given
  PASS  schema: Set answers one value on success, two on a refusal, three on an invalid value
  PASS  schema: a format answering nil falls back to the value's text on the Set line
  PASS  schema: Get answers an interior node for a path with no row, and nil past a leaf
  PASS  schema: every instance member works taken as a bare value, without self
  PASS  schema: Default is a deep copy and nil for an unknown path
  PASS  schema: ApplyDefault writes through Set, and refuses a row with nothing to restore
  PASS  schema: ApplyDefault's table default is stored as a copy
  PASS  schema: resetExempt vetoes a sweep inside a bracket and not a named reset outside one
  PASS  schema: a bracket mutes per-row lines and closes with one line counting changed rows
  PASS  schema: nested brackets are one act, one line, the outermost act and scope
  PASS  schema: a profile reset at any level silences the bracket
  PASS  schema: an error at any level marks the line stopped
  PASS  schema: an unpaired BulkEnd is a no-op and never drives the depth negative
  PASS  schema: BulkRun re-raises the same error value, closes the bracket, marks the line
  PASS  schema: BulkRun hands fn an info table whose profileReset silences the line
  PASS  schema: BulkAdd feeds the open bracket's tally and does nothing outside one
  PASS  schema: a closure row that stores what it already held counts 0 (read-back tally)
  PASS  schema: a raising onChange inside a bracket still counts the write that landed
  PASS  schema: the bulk line honors debugEnabled
  PASS  schema: CountOffDefault counts stored rows off default and honors pred
  PASS  schema: CountOffDefault with no root counts exactly the rows that have a default
  PASS  schema: ResetCounted leaves the count pending for exactly one consumer
  PASS  schema: ResetCounted re-raises unchanged and clears the pending count
  PASS  schema: ConsumeResetCount while a bracket is open closes it with no line
  PASS  schema: a healthy schema validates to zero errors and resolves every stored row
  PASS  schema: each shape error is counted and printed once
  PASS  schema: an unresolvable path is missing; sessionOnly, nil roots and bound rows are exempt
  PASS  schema: types defaults to the four widget types and a host set replaces it
  PASS  schema: Validate with no print still counts, silently
  PASS  schema: one row's errors print in field order, and its missing line prints after them
  PASS  schema: a non-string or empty path is labeled as given and is never a stored path
  PASS  schema: defaultsRoot gets the split parts and the row, and its first may be a string
  PASS  schema: a malformed spec falls back field by field
  PASS  schema: two instances share nothing
  PASS  schema batch: the instance carries SetMany at minor 2
  PASS  schema batch: one invalid entry stores nothing, calls nothing, and names its index
  PASS  schema batch: an unknown path refuses the whole batch
  PASS  schema batch: a normalize refusal inside a batch refuses it whole
  PASS  schema batch: a valid batch stores every entry, runs every onChange, announces once
  PASS  schema batch: every store lands before the first onChange runs
  PASS  schema batch: without announceBatch, announce runs once per write, after the reactions
  PASS  schema batch: with an act the batch is one bracket and one line counting the rows it moved
  PASS  schema batch: a raising onChange still closes the batch's bracket, and the stores stand
  PASS  schema batch: an empty batch stores nothing and answers true
  PASS  schema batch: Set stores normalize's value and hands it to onChange and announce
  PASS  schema batch: normalize answering nil, why refuses with INVALID and stores nothing
  PASS  schema batch: normalize runs only after validate accepts
  PASS  schema batch: a normalize refusal comes before a missing root
  PASS  schema batch: Get hands the instance id to a row's own get
  PASS  schema batch: ApplyDefault(row, id) writes through Set with that id
  PASS  schema batch: a writeThrough path with no row is stored raw, logged and announced
  PASS  schema batch: a row-less path NOT in writeThrough is still refused and never stored
  PASS  schema batch: a writeThrough path that HAS a row takes the row and its validate
  PASS  schema batch: a writeThrough store is a copy, and a missing root still refuses
  PASS  schema batch: a writeThrough write inside a bracket joins its tally, and SetMany takes it
  PASS  schema batch: a malformed writeThrough list keeps only its non-empty strings
  PASS  pool: New hands back an empty pool
  PASS  pool: New hands back a DISTINCT pool each call
  PASS  pool: Acquire builds when the free list is empty, and shows what it hands back
  PASS  pool: a released object is REUSED rather than rebuilt
  PASS  pool: ReleaseAll hides every active object and returns it to free
  PASS  pool: ReleaseAll on an empty pool is a no-op
  PASS  pool: the `before` hook runs on each object, before it is hidden
  PASS  pool: a nested release through the hook empties both levels
  PASS  pool: acquire-release-acquire preserves object identity
  PASS  pool: acquire order survives a release — rank n comes back to rank n
  PASS  pool: NewKeyed hands back an empty keyed pool
  PASS  pool: AcquireKeyed files the object under its key, and shows it
  PASS  pool: AcquireKeyed reuses a released object rather than rebuilding
  PASS  pool: ReleaseAllKeyed hides every active object and returns it to free
  PASS  pool: ReleaseAllKeyed hands the key to the before hook
  PASS  pool: ReleaseAllKeyed on an empty pool is a no-op
  PASS  pool: AcquireKeyed twice on one key replaces nothing and leaks nothing
  PASS  pool: keyed acquire-release-acquire preserves object identity
  PASS  pool: ReleaseAll RAISES on a keyed pool rather than silently recycling nothing
  PASS  pool: ReleaseAll still accepts an ordinary array pool untouched
  PASS  pool: CountsKeyed counts a keyed active map that Counts cannot see
  PASS  pool: the ReleaseAll guard cannot catch keys that are themselves 1..n
  PASS  pool: a keyed release is unaffected by ordering — the key is the mapping
  PASS  item: ItemIDFromLink pulls the id out of a full link
  PASS  item: ItemIDFromLink accepts a bare itemString
  PASS  item: ItemIDFromLink answers nil for anything that is not a link
  PASS  item: QualityFromLink reads the quality out of a legacy |cff hex prefix
  PASS  item: QualityFromLink reads the 11.1.5+ |cnIQ<n> link color
  PASS  item: QualityFromLink retries a quality map that was built empty
  PASS  item: QualityFromLink answers nil for an uncolored or absent link
  PASS  item: QualityFromLink answers nil for a color no quality uses
  PASS  item: QualityLabel prefers the client's localized label
  PASS  item: QualityLabel falls back to the static English map
  PASS  item: QualityLabel defaults to Poor when given nothing
  PASS  item: QualityLabel stringifies a quality it does not know
  PASS  item: LoadItem asks the client to cache the id
  PASS  item: LoadItem fires the callback once the item is loaded
  PASS  item: LoadItem is inert without an id or without the API
  PASS  media: every name in ICONS has a file, and every file has a name
  PASS  media: the icon license ships beside the art
  PASS  media: every name in TEXTURES has a file, and every file has a name
  PASS  media: Texture builds the vendored path, WITHOUT the extension
  PASS  media: the texture keys are the labels a player reads
  PASS  media: every font in FONTS ships with its license
  PASS  media: ICONS holds no duplicate
  PASS  media: Icon builds the vendored path, WITHOUT the extension
  PASS  media: the file behind an icon path is still <name>.tga on disk
  PASS  media: Font builds the vendored path from the catalog's own filename
  PASS  media: an unknown name answers nil rather than a plausible path
  PASS  media: a missing addon name answers nil rather than a path into nowhere
  PASS  media: a consumer that vendors elsewhere passes its own path
  PASS  media: RegisterLSM registers every font and every texture, by catalog name
  PASS  media: RegisterLSM flags the face western+ruRU, so a ruRU client keeps it
  PASS  media: RegisterLSM counts only what LibSharedMedia kept
  PASS  media: RegisterLSM counts a face another copy registered first
  PASS  media: an LSM without the locale bits gets a plain Register
  PASS  media: an LSM without IsValid counts every Register call, as minor 3 did
  PASS  media: no LibSharedMedia is 0 registrations, not an error
  PASS  Widgets.Dropdown draws the host's chevron when it is given one
  PASS  Widgets.Dropdown falls to Blizzard's arrow with no host art
  PASS  Widgets.Dropdown builds its tick markup from the host's check art
  PASS  Two dropdowns can carry different art without either winning
  PASS  Browser menu: Populate shows one row per option and sizes the menu to them
  PASS  Browser menu: a freak label is capped at 320px
  PASS  Browser menu: the menu is never narrower than 90px, nor than its own dropdown
  PASS  Browser menu: rows are POOLED — a shorter dropdown hides the spares, never rebuilds
  PASS  Browser menu: multi-select ticks 'all' exactly when nothing is selected
  PASS  Browser menu: single-select marks the active value and never draws a tick
  PASS  Browser menu: selected rows go gold, the rest keep the value's own color
  PASS  Browser menu: a glyphed row shows its glyph and indents its text past it
  PASS  Browser menu: clicking a multi-select row toggles it and leaves the menu open
  PASS  Browser menu: clicking a single-select row sets the value and closes the menu
  PASS  A glyphed row is painted in the host's face on every pass
  PASS  A host that names no face gets no glyph column
  PASS  A row built with no face at all paints without raising
  PASS  A glyphless row paints without raising even when the host DID name a face
  PASS  A second dropdown repaints the pooled rows in ITS face, not the first one's
  PASS  A second dropdown ticks the pooled rows with ITS art, not the first one's
  PASS  Widgets.CloseMenu hides an open menu
  PASS  Widgets.CloseMenu is a no-op when the menu is already hidden
  PASS  Widgets.CloseMenu is a no-op when no dropdown has ever opened the menu
  PASS  Widgets.CloseMenu leaves the unregistering to the menu's own OnHide
  PASS  A preset row lights up when its own predicate says so
  PASS  A preset row stays dark when its predicate says the selection is something else
  PASS  isActive is asked INSTEAD of the selection set, not alongside it
  PASS  A preset predicate works on a single-select dropdown too
  PASS  Clicking a preset row REPLACES the selection instead of toggling into it
  PASS  A dropdown with no presets toggles exactly as it did at minor 3
  PASS  A preset may override the 'all' sentinel itself
  PASS  The collapsed label takes an active preset's own label
  PASS  A selected value with no option row still counts in the summary
  PASS  A single selection with no option row reads as its raw value
  PASS  An empty selection still reads as the 'all' sentinel's own label
  PASS  One selection that IS in the option list reads as that row's label
  PASS  A preset row survives a REAL row build
  PASS  The menu builds no click-catcher at all
  PASS  An open menu listens for a mouse press anywhere
  PASS  A RIGHT-click outside closes the menu
  PASS  A LEFT-click outside closes the menu
  PASS  Any other mouse button closes it too
  PASS  A press ON the menu does not close it
  PASS  A press on the dropdown that dropped it does not close it
  PASS  A press on a DIFFERENT dropdown still closes the menu
  PASS  An event that is not GLOBAL_MOUSE_DOWN is ignored
  PASS  widgets: CopyWindow answers nil with no client
  PASS  widgets: CopyWindow requires an addon name
  PASS  widgets: CopyWindow fills in the collection's defaults
  PASS  widgets: CopyWindow honors an overridden descriptor
  PASS  widgets: the frame is built once and reused
  PASS  widgets: Show puts the text in the box and leaves it shown
  PASS  widgets: Show sets the text BEFORE it highlights
  PASS  widgets: the frame registers for Esc under its global name
  PASS  widgets: anchorTo is consulted on EVERY show
  PASS  widgets: CopyWindow names the scroll frame when asked (minor 7)
  PASS  widgets: CopyWindow leaves the scroll frame anonymous when not asked
  PASS  widgets: ReorderList reports where a drag landed
  PASS  widgets: ReorderList says nothing for a drag that lands where it started
  PASS  widgets: ReorderList clamps a flat list to its own ends
  PASS  widgets: ReorderList keeps a drag inside its group when a boundary is set
  PASS  widgets: a boundary of 0 or the row count is one flat list
  PASS  widgets: a poll that never reports the button held cannot kill the drag
  PASS  widgets: every start path begins one drag and every end path completes it once
  PASS  widgets: ReorderList carries a copy of the row under the cursor
  PASS  widgets: the insertion line is ANCHORED to the target row
  PASS  widgets: a clamped drag still shows the line, stopped at the divide
  PASS  widgets: Cancel stops a drag in flight and puts the chrome away
  PASS  widgets: Cancel takes every handle OFF the host's frame
  PASS  widgets: a released handle is reused rather than a second one built
  PASS  widgets: a row may be registered with no handle at all
  PASS  widgets: a reused handle drives the LIVE controller, not the one it was built for
  PASS  widgets: the handle takes the hover color and drops it again
  PASS  widgets: a host may override both handle colors
  PASS  widgets: only the handle starts a drag
  PASS  widgets: every registered row gets a fill and four edges
  PASS  widgets: a dimmed row gets the muted variant, and a pooled box does not carry it over
  PASS  widgets: Cancel takes every box OFF the host's frame and back to the pool
  PASS  widgets: rowBox = false draws no box at all
  PASS  widgets: the handle owns the collection's 30px gutter unless the host says otherwise
  PASS  widgets: a box frame that cannot make textures is skipped rather than raising
  PASS  draghandle: it builds a named strip of the published height, hidden, with a label and a mark
  PASS  draghandle: with no parent, and in a process with no CreateFrame, it answers nil
  PASS  draghandle: a frame that cannot make textures is drawn without them rather than raising
  PASS  draghandle: the mark's ART is 8px -- the chevron's INK rather than the chevron's BOX
  PASS  draghandle: the mark is dimmed to the chevron's own tint, and brightens where a click is wired
  PASS  draghandle: a mark with no click behind it does not light up under the cursor
  PASS  draghandle: the mark's FRAME is the full strip height, so the art shrank and the target did not
  PASS  draghandle: the mark is anchored inside the strip's right end at the published inset
  PASS  draghandle: the label is drawn in the same face it is measured in, and one field sets both
  PASS  draghandle: the label is bounded by the reserve on both sides, and never wraps
  PASS  draghandle: a label far longer than the strip cannot reach the mark
  PASS  draghandle: it wears the host's help art, and falls to Blizzard's without any
  PASS  draghandle: Measure is the label plus the reserve each side of it keeps clear, twice
  PASS  draghandle: the reserve is its own terms, so the clearance beside the label can be changed alone
  PASS  draghandle: ApplyWidth floors the natural width at the host's minimum and returns it
  PASS  draghandle: SetLabel re-texts and re-measures, and touches no geometry of its own
  PASS  draghandle: a measurer the client cannot build is a width of 0, not a raise
  PASS  draghandle: the host's numeric guard is what reads every measurement
  PASS  draghandle: dragging the strip moves the host's frame and reports both ends
  PASS  draghandle: canDrag refuses, and a refused drag stops nothing either
  PASS  draghandle: the strip's drag scripts reach the help mark, so the '?' is not a dead zone
  PASS  draghandle: a host that passes no right-click leaves both frames unregistered
  PASS  draghandle: a host that passes one gets it on the strip and on the mark
  PASS  draghandle: no tooltip descriptor means no tooltip at all
  PASS  draghandle: the three bands are a gold title, white body lines, then a spacer and gray footer
  PASS  draghandle: with no surviving footer line there is no spacer either
  PASS  draghandle: the mark shows the same tooltip the strip does
  PASS  draghandle: a body line that answers nil is dropped, not drawn empty
  PASS  draghandle: a footer line is re-evaluated on EVERY hover, not captured at build time
  PASS  draghandle: the owner is the host's call, because for one host it is not a style choice
  PASS  draghandle: a second descriptor gives the mark its own tooltip, owner and anchor
  PASS  draghandle: a descriptor may own by the cursor while its neighbor owns by the frame
  PASS  draghandle: a line may carry its own color, so a gold line in a white band stays gold
  PASS  draghandle: the strip is a plain Button with a fill, never a BackdropTemplate
  PASS  draghandle: a host with its own edge painter gets its own pixels
  PASS  draghandle: DragHandle is at minor 3, the close mark's minor
  PASS  draghandle: a spec with no onClose draws exactly the minor-2 strip, number for number
  PASS  draghandle: onClose builds an X the same frame and art size as the '?', immediately left of it
  PASS  draghandle: with no closeIcon the X falls to Blizzard's stop button
  PASS  draghandle: the X widens the reserve on BOTH sides, so the label stays centered
  PASS  draghandle: a left click on the X calls onClose once, and a right click does not
  PASS  draghandle: an X on a host with no right-click registers the left click alone
  PASS  draghandle: the X takes the strip's drag scripts, so a drag that starts on it moves the frame
  PASS  draghandle: the X brightens under the cursor and shows closeTooltip, owned by the X
  PASS  draghandle: an X with no closeTooltip shows the strip's, and follows a cursor owner
  PASS  draghandle: an X on a strip whose '?' could not be built sits where the '?' would
  PASS  reorder: a host OnUpdate on a row frame survives a drag start and end
  PASS  reorder: a row frame with no OnUpdate is never given one
  PASS  reorder: two lists with different lineColor show their own color on one pooled container
  PASS  reorder: the drop line is released after Cancel
  PASS  reorder: the drop line goes back at the drop, and the next drag reuses it
  PASS  dbg: FormatPlain wraps the tag in brackets with single-space separators
  PASS  dbg: FormatPlain tolerates a nil tag
  PASS  dbg: FormatColored colors the timestamp and tag; pipe and content default
  PASS  dbg: both formatters are reachable on an instance as well as on the library
  PASS  dbg: the window title is the host's, with the library's suffix appended
  PASS  dbg: a host can override the title suffix
  PASS  dbg: Add appends the plain form to the buffer and is never gated on the flag
  PASS  dbg: the cap is 1500 and the message frame is held to the same number
  PASS  dbg: the buffer is capped, dropping the oldest line
  PASS  dbg: the buffer stays a dense array of plain strings
  PASS  dbg: Clear wipes the buffer and works before the window was ever built
  PASS  dbg: BufferSize, LastLine and FindLine answer without reaching into .buffer
  PASS  dbg: at 1499 lines every public reader answers the newest MAX_BUFFER
  PASS  dbg: at 1500 lines every public reader answers the newest MAX_BUFFER
  PASS  dbg: at 1501 lines every public reader answers the newest MAX_BUFFER
  PASS  dbg: at 1600 lines every public reader answers the newest MAX_BUFFER
  PASS  dbg: the 1501st line drops the first
  PASS  dbg: 1564 adds cost at most one compaction, not one table.remove per line
  PASS  dbg: the raw buffer holds at most MAX_BUFFER + 64 lines, and compacts in order
  PASS  dbg: the status line counts what the readers answer, not the raw array
  PASS  dbg: the sink routes the first arg as the [tag] and every vararg through safeToString
  PASS  dbg: the sink is a no-op, and does no work at all, when logging is off
  PASS  dbg: the sink is dot-callable, because host call sites bind it bare
  PASS  dbg: the sink survives a format the stringified args cannot satisfy
  PASS  dbg: an ordinary format is NOT routed through the fallback
  PASS  dbg: SetEnabled writes the flag through the host, not into the library
  PASS  dbg: SetEnabled normalizes a truthy value to a boolean
  PASS  dbg: enabling acks in green, brackets the session, then adds the [Init] summary
  PASS  dbg: disabling acks in red and the bracket line still lands after the flag flips
  PASS  dbg: disabling adds no [Init] summary
  PASS  dbg: a host with no initSummary gets the bracket line and nothing else
  PASS  dbg: the header toggle click flips the flag through SetEnabled
  PASS  dbg: Show builds and shows; Hide and IsShown never build
  PASS  dbg: Toggle builds the window on its first call
  PASS  dbg: showing and hiding the console tells the host
  PASS  dbg: two instances own separate buffers and separate frames
  PASS  dbg: the copy text is the whole buffer, in order, newline-joined
  PASS  dbg: Add sends the COLORED form to the console and the plain one to the buffer
  PASS  dbg: the window degrades to nothing when CreateFrame is unavailable
  PASS  dbg: ConsoleCheckbox get reflects window visibility, not the logging flag
  PASS  dbg: ConsoleCheckbox set shows and hides without touching the logging flag
  PASS  dbg: the ConsoleCheckbox tooltip names the host's own slash command
  PASS  dbg: a host with no slash command gets a tooltip that does not mention one
  PASS  dbg: New requires a name, a title, a font and an isEnabled/setEnabled pair
  PASS  dbg: a host that overrides a string gets its own wording
  PASS  dbg: DebugLog re-exports Core's close button so a host has one factory, not two
  PASS  dbg: a newer Core loading after DebugLog supplies the console's close button
  PASS  dbg: an instance built after the upgrade draws the newer Core's button
  PASS  dbg: Add renders a secret message as the sentinel
  PASS  an L whose metatable synthesizes every key does NOT mask the module's own strings
  PASS  a REAL entry in an L that also has a fallback still overrides
  PASS  a plain L table overrides exactly as before
  PASS  dbg: with no makeCloseButton, BOTH windows close with Core's x
  PASS  dbg: the default chrome IS the Ka0s window edge, on both windows
  PASS  dbg: a host can supply its own skin function, for both windows
  PASS  dbg: the host's skin function runs AFTER the Hide and the Esc wiring
  PASS  dbg: a host that supplies no skin function still gets the library's own
  PASS  dbg: a host can supply its own close-button factory, for both windows
  PASS  dbg: the host's close button actually closes the window
  PASS  dbg: a close-button factory returning nil is survivable, as Core's own is
  PASS  dbg: the title-bar offsets are derived from the close button's width
  PASS  dbg: with addonName the title bar draws icons, and they are narrower than the words
  PASS  dbg: an icon control carries NO tooltip
  PASS  dbg: the close button is told the addon name too, not just copy and clear
  PASS  dbg: the library's own close forwarder passes the name through to Core
  PASS  dbg: no addonName is the minor-8 title bar, word for word
  PASS  dbg: an addonName the art does not answer for falls back to words
  PASS  dbg: a wider host close button pushes Copy and Clear out of its way
  PASS  dbg: a close button with no measurable width falls back to the library's own
  PASS  dbg: with no close button at all the offsets are still the minor-3 defaults
  PASS  dbg: the copy window is built by Widgets.CopyWindow, not by this file
  PASS  dbg: the converged copy window keeps its named scroll frame
  PASS  dbg: the converged copy window keeps its global frame name
  PASS  dbg: the copy window still shows the whole buffer, in order
  PASS  dbg: the copy window re-anchors to the console instead of a fixed center
  PASS  sl: an empty message runs the host's config verb (minor 11), printing no help
  PASS  sl: whitespace-only input is treated as empty
  PASS  sl: a host with no config verb still gets the help index for an empty message
  PASS  sl: `help` prints the help index
  PASS  sl: an unknown verb names it, then prints the help index
  PASS  sl: the verb is lowercased but the argument keeps its case
  PASS  sl: an alias is rewritten to its target verb
  PASS  sl: a handler receives the rest of the line, not the verb
  PASS  sl: New requires a slash prefix and a commands table
  PASS  sl: a help row is gold command, single-spaced em dash, white description
  PASS  sl: HelpRows indents for chat; LandingRows does not
  PASS  sl: help rows name each verb with the host's own slash prefix
  PASS  sl: the help header carries the version and the chat alias
  PASS  sl: a host with no chat alias gets a header without the alias clause
  PASS  sl: no rendered line ends in a colon
  PASS  sl: FormatKV is a gold key, ' = ', a white value, and no trailing colon
  PASS  sl: FormatValue renders every schema type the library knows
  PASS  sl: a color channel the stored table omits falls back per channel, alpha to 1 and RGB to 0
  PASS  sl: a number row with no fmt renders bare
  PASS  sl: a row whose value does not fit its declared type falls through to the generic renderer
  PASS  sl: FormatValue renders a secret as the sentinel on every formatting branch
  PASS  sl: FormatValue reads a POSITIONAL color as well as a named-key one
  PASS  sl: a positional color with a secret component still renders the sentinel
  PASS  sl: a host color codec round-trips through set and its echo
  PASS  sl: CliReset's echo uses the host color codec too
  PASS  sl: a guarded FormatValue still survives the FormatKV string.format around it
  PASS  sl: SplitVerb lowercases the verb and preserves the remainder's case
  PASS  sl: SplitVerb keeps the remainder's internal spacing
  PASS  sl: SplitVerb answers two empty strings for empty and nil input
  PASS  sl: SplitVerb answers an empty remainder for a bare verb
  PASS  sl: FindCommand returns the whole triple for a matching name
  PASS  sl: FindCommand compares verbatim and answers nil for a miss
  PASS  sl: FindCommand answers nil rather than raising on a missing list
  PASS  sl: CommandRows renders one row per entry through the shared formatter
  PASS  sl: CommandRows defaults to no indent and applies the one it is given
  PASS  sl: CommandRows answers an empty list rather than raising on a missing table
  PASS  sl: HelpRows and LandingRows render through CommandRows
  PASS  sl: ParseBool accepts the same eight words the error string advertises
  PASS  sl: ParseBool answers nil, never false, for a non-boolean word
  PASS  sl: booleans accept the whole human vocabulary
  PASS  sl: a junk boolean is rejected and the accepted words are listed
  PASS  sl: a number is clamped to the row's range rather than rejected
  PASS  sl: a non-numeric value for a number row is rejected
  PASS  sl: a string is validated against its enum, case-sensitively
  PASS  sl: an enum declared as an ordered array is offered in declaration order
  PASS  sl: an ordered array supplied as a function is evaluated at parse time
  PASS  sl: a numeric dropdown rejects an out-of-list value rather than clamping it
  PASS  sl: a number row with no values list still clamps to min/max
  PASS  sl: a string row with no values list accepts free text
  PASS  sl: a free-text string row keeps every word of a multi-word value
  PASS  sl: a string enum accepts an entry that contains spaces, in both enum shapes
  PASS  sl: a string value is trimmed at both ends before it is stored or validated
  PASS  sl: an empty or blank string value is still refused with 'expected a value'
  PASS  sl: bool, number and color rows still read tokens exactly as before
  PASS  sl: set stores a multi-word free-text value whole, through the dispatcher
  PASS  sl: a key SET labels its entries with its keys, not with 'true'
  PASS  sl: an enum supplied as a function is evaluated at parse time
  PASS  sl: a color parses r g b with an optional alpha
  PASS  sl: a color given in 0-255 is rescaled, and all three channels together
  PASS  sl: a color missing a channel is rejected with the expected form
  PASS  sl: an unknown row type is rejected by name
  PASS  sl: list groups rows under the host's own group keys, indented
  PASS  sl: the list keeps its own colors — green header, azure group headings
  PASS  sl: list says so when nothing is registered
  PASS  sl: get echoes the canonical path and the stored value
  PASS  sl: a stored false renders as false, not as nil
  PASS  sl: get with no path prints usage; an unknown path says so
  PASS  sl: set writes through the host and echoes what was STORED, not what was typed
  PASS  sl: set with no path points at the list verb
  PASS  sl: a rejected value is not written, and the reason is a second, indented line
  PASS  sl: set accepts a value made of several tokens
  PASS  sl: reset restores one setting to its default
  PASS  sl: reset leaves every other setting alone
  PASS  sl: reset with no path prints usage; an unknown path says so
  PASS  sl: reset does not lowercase its argument
  PASS  sl: resetall applies every row's default
  PASS  sl: resetall brackets its walk, and acknowledges after the bracket closes
  PASS  sl: a row that raises inside resetall still closes the bracket; the error propagates and nothing is acknowledged
  PASS  sl: resetall with no applyDefault still brackets, and counts zero rows written
  PASS  sl: resetall hands bulkEnd an info whose profileReset is false, and the host logs [Set] reset all: N rows
  PASS  sl: resetall with NO bracket is minor 7's walk — an error escapes with its own stack
  PASS  sl: version prints one line and nothing else
  PASS  sl: the annotator fires on list, get and set — and on nothing else
  PASS  sl: the annotation follows the colored pair rather than interrupting it
  PASS  sl: with no annotator set, nothing is appended
  PASS  sl: Slash refuses to register without Core
  PASS  sl: an L whose metatable synthesizes every key does NOT mask the module's strings
  PASS  sl: a REAL entry in an L that also has a fallback still overrides
  PASS  sl: a plain L table overrides exactly as before
  PASS  slash: a host can supply its own value formatter for a type the library does not know
  PASS  slash: the format hook reaches the get, set and reset echoes too
  PASS  slash: a host with no format hook renders exactly as it always did
  PASS  slash: the format hook takes precedence over the color codec, and gets the raw stored value
  PASS  slash: format beats colorDecode at the get, set and reset echoes, and colorEncode still runs
  PASS  sl: the refusal line's shape is the collection's, down to the color and the dash
  PASS  sl: an absent isEnabled leaves the dispatcher behaving exactly as it did at minor 11
  PASS  sl: isEnabled without brandName is refused at New, not rendered as 'nil is disabled'
  PASS  sl: a FEATURE verb answers exactly one refusal line and reaches no write seam
  PASS  sl: the reserved verbs and the whole schema CLI answer NORMALLY while disabled
  PASS  sl: the bare command opens the panel, and a TYPO gets the unknown-command line
  PASS  sl: an alias onto a gated verb is refused, and an alias onto a live one is honored
  PASS  sl: enable answers normally and is the way back
  PASS  sl: disable ECHOES the write rather than refusing, and is idempotent
  PASS  sl: help prints the full index with the refusal line under its header, unindented
  PASS  sl: help enabled prints no refusal line at all
  PASS  sl: liveVerbs defaults to the standard's twelve reserved verbs and is overridable as DATA
  PASS  sl: a reserved verb the host never shipped is not refused, in either state
  PASS  sl: the gate is asked per dispatch, so a value that changes mid-session is honored
  PASS  sl: the refusal wording is NOT reachable through the locale override
  PASS  sl refusal: a set answering false, err, why prints INVALID and the reason, and no echo
  PASS  sl refusal: an err that is not the INVALID line itself is printed indented, before why
  PASS  sl refusal: a refusal with no err and no why prints the INVALID line alone
  PASS  sl refusal: a set answering true echoes the stored value
  PASS  sl refusal: a set answering nothing still echoes, as at minor 14
  PASS  sl refusal: CliReset prints NO_DEFAULT when applyDefault answers exactly false
  PASS  sl refusal: CliReset echoes when applyDefault answers nil or true
  PASS  launcher: New refuses a descriptor missing name, icon or openSettings
  PASS  launcher: ONE object, of type 'launcher', carrying the host's own icon
  PASS  launcher: both registrations use the addon's folder name, and a host may relabel
  PASS  launcher: Register is idempotent, so a second call builds no second button
  PASS  launcher: IsRegistered is false until BOTH halves are wired
  PASS  launcher: the library ALWAYS draws the tooltip, and a host need pass nothing
  PASS  launcher: the tooltip title is the label, and the version where one is passed
  PASS  launcher: the tooltip's Enabled line is green Yes or red No, read from isEnabled
  PASS  launcher: the click hints are fixed, in either state, whatever retired fields are passed
  PASS  launcher: every combination of state and status lines draws the fixed shape
  PASS  launcher: tooltip Locked and Test mode values are green for Yes/On, red for No/Off
  PASS  launcher: the host's tooltip lines are appended ONCE, below the status, above the hints
  PASS  launcher: tooltip status is read on EVERY show, never cached
  PASS  launcher: a raising tooltip accessor or host hook costs its own line, not the tooltip
  PASS  launcher: a tooltip argument with no AddLine is left alone
  PASS  launcher: every tooltip string goes through the descriptor's L, rawget-guarded
  PASS  launcher: minor 4 is live
  PASS  launcher: left-click opens the settings panel, enabled or disabled
  PASS  launcher: onClick, leftClickLabel, disabledLine and slash are retired and ignored
  PASS  launcher: right-click opens the client's context menu, titled with the label
  PASS  launcher: the menu carries one checkbox per supplied toggle, in the one order
  PASS  launcher: an accessor without its toggle, or a toggle without its accessor, draws nothing
  PASS  launcher: each checkbox shows the state its accessor answers when the menu opens
  PASS  launcher: each entry toggles through its host function exactly once
  PASS  launcher: while disabled, Enabled stays live and the rest are grayed with the note
  PASS  launcher: a grayed entry clicked anyway calls no handler and writes nothing
  PASS  launcher: a host with no isEnabled has nothing grayed and no Enabled entry
  PASS  launcher: with no MenuUtil, right-click degrades to the settings panel
  PASS  launcher: a raising toggle or menu API is reported and never escapes
  PASS  launcher: a raising accessor costs its checkmark, not the menu
  PASS  launcher: every menu string goes through the descriptor's L, rawget-guarded
  PASS  launcher: the host's OWN minimap table is handed to LibDBIcon, never a copy
  PASS  launcher: the minimap table is resolved at REGISTER time, not at New
  PASS  launcher: IsShown reads LibDBIcon's own `hide` key and inverts it
  PASS  launcher: SetShown writes `hide` and drives LibDBIcon's Show / Hide
  PASS  launcher: the debug seam reports under the Launcher tag
  PASS  launcher: with no LibDataBroker there is no object, and nothing raises
  PASS  launcher: with no LibDBIcon the broker plugin still registers; the button does not
  PASS  launcher: two Register calls with no LibDBIcon print NO_ICON once
  PASS  launcher: STRINGS carry no [LibKa0s] tag, because the host printer adds its own
  PASS  launcher: SetShown still records the player's choice where LibDBIcon is absent
  PASS  launcher: a descriptor whose minimap answers no table refuses the button and says why
  PASS  launcher: a name LibDataBroker already holds takes that object rather than none
  PASS  launcher: a host locale overrides a report, and a key-echoing fallback does not
  PASS  launcher: with no descriptor print, a report reaches the chat frame
  PASS  options: the major registers all three of its files
  PASS  options: an instance carries the shell, the widget makers and the scroll patch
  PASS  options: two instances own separate panel registries
  PASS  options: CreatePanel returns a ctx wired to a panel, a body and an empty refresher list
  PASS  options: CreatePanel names the panel with the plain title for the category tree
  PASS  options: CreatePanel starts the panel hidden and registers it
  PASS  options: the header title takes the parent breadcrumb, and isMain opts out
  PASS  options: __panelFor finds a registered page by key
  PASS  options: SelectTab sets a rendered page's active tab and refreshes
  PASS  options: SelectTab refreshes only the target page, not every rendered page
  PASS  options: SelectTab reports false for a page that has never been rendered
  PASS  options: CreatePanel only DECLARES the Defaults button, never builds it
  PASS  options: CreatePanel records no Defaults intent when the page did not ask
  PASS  options: EnsureDefaultsButton builds it once, wires the parked handler, then no-ops
  PASS  options: EnsureDefaultsButton is a safe no-op without AceGUI and on a nil panel
  PASS  options: EnsureDefaultsButton leaves a panel that never wanted one alone
  PASS  options: EnsureDefaultsButton survives a vendored copy whose widget makers never attached
  PASS  options: RestoreDefaults resets every row on the named page and no other
  PASS  options: RestoreDefaults runs the ctx refreshers, and survives one that throws
  PASS  options: RestoreDefaults on a page with no rows is a harmless no-op
  PASS  options: RestoreDefaults resets a page across EVERY filter value, unlike RenderSchema
  PASS  options: RestoreAllDefaults resets every row, then fires the host's afterRestoreAll
  PASS  options: with resetProfile supplied, only the sessionOnly rows are walked
  PASS  options: resetProfile runs BEFORE afterRestoreAll, which runs before the refresh
  PASS  options: the narrowing is applied BEFORE skipRestoreAll is consulted
  PASS  options: with NO resetProfile the reset is exactly what it always was
  PASS  options: RestoreAllDefaults fires afterRestoreAll BEFORE refreshing the panels
  PASS  options: RestoreAllDefaults honors the host's skipRestoreAll veto
  PASS  options: RefreshAllPanels runs every registered panel's refreshers, isolating a thrower
  PASS  options: registered page builders run in registration order, once, at CreateOptionsPanel
  PASS  options: CreateOptionsPanel hands the host the AceGUI it resolved
  PASS  options: CreateOptionsPanel says so and returns when AceGUI is missing
  PASS  options: the main canvas is registered under the host's brand
  PASS  options: the main page's body is deferred to its first OnShow, and built once
  PASS  options: a raising page builder costs that page and no other
  PASS  options: a page registered after the build is built immediately
  PASS  options: SetRenderer draws on first show, and not again
  PASS  options: a panel shown during combat is covered, not drawn, and the window is NOT closed
  PASS  options: a raising renderer is reported, not propagated
  PASS  options: RefreshScalars re-syncs a shown page and flags a hidden one dirty
  PASS  options: a dirty hidden page re-renders on its next show
  PASS  options: the two tiers differ — one re-renders, the other only re-syncs
  PASS  options: RefreshPanel touches ONE page, on both tiers
  PASS  options: RefreshPanel defers a hidden page to its next show
  PASS  options: RefreshPanel ignores a non-ctx rather than raising
  PASS  options: a ctx that never went through SetRenderer keeps the old ungated behavior
  PASS  options: OpenOptionsPanel REFUSES under combat and does not defer-and-replay
  PASS  options: OpenOptionsPanel opens the registered category out of combat
  PASS  options: :New refuses a descriptor with no mainPanelName
  PASS  options: a host that omits print still sees the combat refusal in the chat frame
  PASS  options: CreateOptionsPanel is idempotent in both the category and the refreshers
  PASS  options: OpenOptionsPanel is a silent no-op before CreateOptionsPanel has run
  PASS  options: LSMValues returns a DEFERRED closure, not a snapshot
  PASS  options: LSMValues offers a None placeholder rather than an empty list
  PASS  options: __PatchLSM30Border is published on the library, not on an instance
  PASS  options: __PatchLSM30Border registers once and the second call is a no-op
  PASS  options: the patched constructor hides the preview tile and re-anchors the bar
  PASS  options: __PatchLSM30Border stays armed while the widget is absent
  PASS  options: EnsureScroll is lazy, created once, and patched
  PASS  options: OptionsScroll.lua owns the font preload; without it a show still renders
  PASS  options: the scrollbar patch is idempotent
  PASS  options: FixScroll disables the bar when the content fits, enables it when it does not
  PASS  options: OnRelease restores AceGUI's own FixScroll and clears the marker
  PASS  options: CreatePanel stamps the three Blizzard canvas callbacks
  PASS  options: OnCommit and OnRefresh are inert and safe to call
  PASS  options: OnDefault forwards to a defaultsOnClick parked AFTER CreatePanel
  PASS  options: OnDefault and the header Defaults button run the SAME action
  PASS  options: a page with no defaults action still has a callable, inert OnDefault
  PASS  options: every lib.LAYOUT key is either published on the instance or annotated internal
  PASS  options: the published layout scalars are the instance's own, never lib.LAYOUT itself
  PASS  options: the landing constants are published on lib.LAYOUT at the promoted values
  PASS  options: LANDING_GAP_HEAD and SECTION_BOTTOM_SPACER are the same gap
  PASS  options: a host wires the landing body itself, through the buildMain it always had
  PASS  options: the shell installs no main renderer of its own, whatever else the descriptor carries
  PASS  options: the shell never writes buildMain onto the host's descriptor
  PASS  options: a descriptor with no buildMain draws no main body
  PASS  options: a disabled bar parks the thumb, dims it, and disables both step buttons
  PASS  options: an enabled bar tints the thumb white and enables both step buttons
  PASS  options: a state change fires once, not once per FixScroll
  PASS  options: a nameless scrollbar resolves no step buttons and still patches
  PASS  options: a fresh panel reserves no chrome, so its scroll sits where it always did
  PASS  options: reserving chrome pushes the scroll down by exactly that many pixels
  PASS  options: reserving chrome AFTER the scroll exists re-anchors the live scroll
  PASS  options: ClearScroll leaves the reserved band alone
  PASS  options: ClearScroll resets BOTH heading trackers
  PASS  options: RestoreDefaults brackets its page walk — begin, every row, end with the count, then the refresh
  PASS  options: RestoreAllDefaults brackets the whole act — rows, afterRestoreAll — and refreshes after the end
  PASS  options: with resetProfile the bracket spans the session rows, the profile reset and the hook
  PASS  options: a row that raises mid-walk still closes the bracket, then the error propagates
  PASS  options: the bracket closes when afterRestoreAll or bulkBegin itself raises
  PASS  options: either half of the bracket works alone
  PASS  options: a host mutes its seam's [Set] inside the bracket and logs ONE line — the documented worked example, a page reset
  PASS  options: a profile-reset RestoreAllDefaults is ONE line, the profile handler's — bulkEnd's info says so and the host adds nothing
  PASS  options: without resetProfile, RestoreAllDefaults' info.profileReset is false and the host logs [Set] reset all: N rows, N the rows actually written
  PASS  options: a resetProfile that raises leaves info.profileReset false and hands bulkEnd the error
  PASS  options: with NO bracket the walk is exactly minor 15's — same calls, same order, and an error escapes with its own stack
  PASS  fontpreload: the first panel OnShow loads each LSM font path exactly once
  PASS  fontpreload: the strings live on one shown, full-alpha frame parented to UIParent
  PASS  fontpreload: a second show, and a second host's panel, load nothing new
  PASS  fontpreload: a font registered after the preload is loaded when it registers
  PASS  fontpreload: no registration callback before the first show
  PASS  fontpreload: no LibSharedMedia is no error and creates nothing
  PASS  fontpreload: no CreateFrame is no error, and the next show can still load
  PASS  fontpreload: a SetFont that raises costs that face and nothing else
  PASS  fontpreload: a show locked for combat loads nothing; the next show does
  PASS  fontpreload: a page with no renderer loads on its show too
  PASS  fontpreload: the main page loads on its first show, with a buildMain and without
  PASS  widgets: the cross-slice layout constants are published on the instance
  PASS  widgets: a bool row renders a CheckBox labeled and seeded from the schema
  PASS  widgets: clicking a checkbox writes through the descriptor's set
  PASS  widgets: a checkbox registers a refresher that re-reads after an external change
  PASS  widgets: every widget gets tooltip callbacks wired from the schema desc
  PASS  widgets: relativeWidth is applied when given, full width otherwise
  PASS  widgets: SessionCheckbox reads and writes the caller's get/set, never the store
  PASS  widgets: a number row renders a Slider carrying the schema's range and step
  PASS  widgets: a slider falls back to the row default when the stored value is not a number
  PASS  widgets: releasing a slider snaps the value to the row's step
  PASS  widgets: slider snapping is relative to the row's min, not to zero
  PASS  widgets: a string row with values renders a Dropdown, sorted alphabetically by default
  PASS  widgets: a row with explicit `sorting` keeps that order instead of alphabetizing
  PASS  widgets: a dropdown falls back to a plain Dropdown when its dialogControl is unregistered
  PASS  widgets: a dropdown uses its dialogControl widget when that IS registered
  PASS  widgets: a dropdown writes the chosen value, and its refresher re-applies the LIST
  PASS  widgets: a dropdown built from an ordered array keeps declaration order
  PASS  widgets: a key set labels its entries with its keys, not with 'true'
  PASS  widgets: the dropdown's options and the CLI's allowed values agree, in both shapes
  PASS  widgets: a color row opts OUT of alpha by declaring it, and cannot before
  PASS  widgets: a tooltip body comes from `tooltip`, with `desc` still accepted
  PASS  widgets: a slider does not commit on drag by default
  PASS  widgets: sliderCommit = 'change' commits on drag, throttled, last value wins
  PASS  widgets: commitOn on a row overrides the descriptor default, both ways
  PASS  widgets: a raising row costs that row and no other
  PASS  widgets: RenderGrid lays arbitrary items out two per row
  PASS  widgets: RenderGrid gives a wide item its own full-width row
  PASS  widgets: RenderGrid guards each item the way RenderRows guards each row
  PASS  widgets: ChoiceGrid draws a heading, a header line and one line per row
  PASS  widgets: ChoiceGrid draws skipRender rows, and a custom label header
  PASS  widgets: ChoiceGrid lights the cell holding the stored value and only that one
  PASS  widgets: a ChoiceGrid click writes the column value and re-syncs the whole line
  PASS  widgets: clicking the lit ChoiceGrid cell keeps it lit and writes nothing
  PASS  widgets: a ChoiceGrid value outside the columns lights no cell
  PASS  widgets: ChoiceGrid radios re-read the store when the refreshers run
  PASS  widgets: ChoiceGrid reads and writes a path-less row through its own get/set
  PASS  widgets: ChoiceGrid cells are ordinary checkboxes, never radios and never painted
  PASS  widgets: a CheckBox recycled after a ChoiceGrid comes back with an untinted check (G-2)
  PASS  widgets: ChoiceGrid disables a row's cells by its disabledIf
  PASS  widgets: a ChoiceGrid drawn inside a disabled render is disabled with it
  PASS  widgets: ChoiceGrid spec.disabled disables every cell for the call only
  PASS  widgets: a ChoiceGrid label carries the row's tooltip
  PASS  widgets: a ChoiceGrid row that raises costs its own line, not the grid
  PASS  widgets: ChoiceGrid with no AceGUI draws nothing
  PASS  widgets: an extraColumn draws a header cell and a clickable per-row link, wired to onClick
  PASS  widgets: an extraColumn's nil cell draws a blank of the same width, so rows stay aligned
  PASS  widgets: an extraColumn cell that raises costs only that cell, not the line or the grid
  PASS  widgets: an extraColumn cell with a non-function onClick draws without wiring a handler
  PASS  widgets: an extraColumn narrows the label column, and the line still fits one Flow row
  PASS  widgets: with no extraColumn, ChoiceGrid's line shape is unchanged
  PASS  ResolveId: a number is an id, and a known one carries its name and icon
  PASS  ResolveId: every link form resolves, for its own kind only
  PASS  ResolveId: a spell name the client knows resolves to its id
  PASS  ResolveId: a name the client cannot look up is found among the host's candidates
  PASS  ResolveId: two candidates with the name are ambiguous, one listed twice is not
  PASS  ResolveId: nothing typed is empty, and an unknown name is not found
  PASS  ResolveId: a custom kind's resolver is handed everything typed
  PASS  ResolveId: with no client APIs a number still resolves and a name finds nothing
  PASS  IdInput: an edit box and an Add button share a line, with a status line under them
  PASS  IdInput: the Add button is the height of the box it sits beside, not AceGUI's default
  PASS  IdInput: Enter with a valid name adds it once and clears the box
  PASS  IdInput: the Add button submits what was typed
  PASS  IdInput: a name that resolves to nothing says so inline and adds nothing
  PASS  IdInput: an ambiguous name asks for the id, in the kind's own plural
  PASS  IdInput: the host can reword the button and the messages
  PASS  IdInput: a raising onAdd is reported, and the box keeps its text
  PASS  IdInput: the box and status line are cleared before onAdd, so onAdd may redraw the page
  PASS  IdInput: drawn inside a disabled render, or with spec.disabled, it is disabled
  PASS  IdInput: with no AceGUI it draws nothing
  PASS  IdList: one line per entry -- icon, name and gray id, then Remove or a checkbox
  PASS  IdList: an entry's note is drawn under its name, and only when it has one
  PASS  IdList: an empty-string or non-string note draws nothing
  PASS  IdList: an entry's suffix is drawn inside the label, after the gray id and in the same gray
  PASS  IdList: an entry with no suffix renders exactly as it did at minor 24
  PASS  IdList: a suffixed entry still pairs up at two columns -- it is not a full-width row
  PASS  IdList: a suffix and a note on one entry -- the note wins its line, the suffix stays inline
  PASS  IdList: a suffix is concatenated, so a % or a |c in it reaches the client as written
  PASS  IdList: with no columns option each entry has its line to itself, at minor 23's widths
  PASS  IdList: columns = 2 packs entries two to a line, row-major, at half the widths
  PASS  IdList: columns = 2 in the icon style halves the name and leaves the X alone
  PASS  IdList: an odd entry count leaves the last line half filled, not stretched
  PASS  IdList: inside a two-column list a noted entry takes a full-width line of its own
  PASS  IdList: a columns value that is not a usable count is floored, clamped, or read as 1
  PASS  IdList: at two columns a failing entry costs itself, not the entry beside it
  PASS  IdList: at two columns the FIRST entry of a row fails without stranding the row
  PASS  IdList: columns is capped at two, and the cap's arithmetic is the label's and the X's
  PASS  IdList: a content width two columns cannot pay for draws one, not a broken grid
  PASS  IdList: a content width that covers the floor keeps the columns the host asked for
  PASS  IdList: the default style falls back on the LABEL's floor, which is its only one
  PASS  IdList: a width that cannot be measured leaves the column count exactly as it was
  PASS  IdList: the X's frame is wider than its art, absolute, at every column count
  PASS  IdList: a gutter separates each entry from the next, and only at more than one column
  PASS  IdList: at more than one column an entry name is one line tall, never wrapped
  PASS  IdList: a list where nothing carries help draws no marks at all
  PASS  IdList: every entry gets a mark once ANY entry carries help
  PASS  IdList: a mark with nothing to say is dimmed and answers no tooltip
  PASS  IdList: a string help reads as one line
  PASS  IdList: the help mark's width comes out of the NAME
  PASS  IdList: a helped list still gets two columns on a canvas that pays for them
  PASS  IdList: the mark is drawn big enough to read, in a frame with the X's 5px ring
  PASS  IdList: the mark draws this library's own info art when the host names itself
  PASS  IdList: the art ladder falls back, and a host that names its own art keeps it
  PASS  IdList: a help level tints the mark, and an entry that names none keeps its gold
  PASS  IdList: an unknown level draws the default, and a hover leaves a mark its own color
  PASS  IdList: a one-column list still wraps, and now lights too (minor 28)
  PASS  IdList: the no-wrap FontString is put back when AceGUI takes the widget back
  PASS  IdList: release clears the markers, so a pooled label cannot answer for the next list
  PASS  IdList: a label with no FontString still has its markers cleared
  PASS  IdList: at more than one column the hovered entry is lit, so the tooltip has an owner
  PASS  IdList: a multi-column tooltip hangs off the row, not over the column beside it
  PASS  IdList: Remove and a toggle call the host back, and Remove asks for a rebuild
  PASS  IdList: an add through its input reaches onAdd and rebuilds the list
  PASS  IdList: with no ctx.rebuild the library's structural refresh redraws it
  PASS  IdList: an empty list shows the host's empty text
  PASS  IdList: an uncached item asks to load, and the list redraws once its name lands
  PASS  IdList: an item's name is colored by its quality; a spell's and a currency's are not
  PASS  IdList: an item with no quality yet, or no palette for it, is drawn uncolored
  PASS  IdList: uncached items load as one batch -- one timer and one rebuild, however many
  PASS  IdList: an item not cached by the check is asked for again, a bounded number of times
  PASS  IdList: an entry's label shows the client's own tooltip for it
  PASS  IdList: a host kind with base = "item" wears the item kind's color, tooltip and loads
  PASS  IdList: a host kind without base, or with a base no library kind has, is drawn as before
  PASS  IdList: a based spell kind draws as a spell and keeps its own tooltip
  PASS  IdList: a raising entries() is reported and still draws the input
  PASS  IdList: drawn disabled, every Remove and checkbox is disabled
  PASS  IdList: with no AceGUI it draws nothing
  PASS  ResolveId: a client name hit another candidate shares its name with is ambiguous
  PASS  UnnamedCandidates: the item candidates the client cannot name yet, each once, capped
  PASS  IdInput: a name among uncached candidates is looked up, and added once it lands
  PASS  IdInput: a lookup waits for every candidate it asked for, then refuses a shared name
  PASS  IdInput: a lookup that never lands gives up after a bounded wait, with the honest reason
  PASS  IdInput: a second submit, a changed box or a released box drops a pending lookup
  PASS  IdInput and IdList: built with item candidates, they ask for the unnamed ones up front
  PASS  IdInput: a name that finds nothing says where names work, per kind; the hint is exported
  PASS  IdInput: the looking line can be reworded
  PASS  IdInput: a client hit on one rank waits for the uncached ranks, then refuses the name
  PASS  IdInput: a name hit waits on unnamed candidates, then adds; a number or a link never waits
  PASS  IdInput: a host kind with resolve, loads and info is looked up, and refuses a shared name
  PASS  IdInput: a second submit of the same text replaces the pending lookup
  PASS  IdInput: ids a lookup could not load are skipped, so later candidates get their turn
  PASS  IdInput: a lookup runs at most five windows of 200; the next Enter carries on past them
  PASS  IdInput and IdList: pre-warm moves past the ids it has asked for, and reads each id once
  PASS  widgets: a string row asking for an EditBox gets one, not a dropdown
  PASS  widgets: an edit box commits on OnEnterPressed and re-reads on refresh
  PASS  widgets: a color row renders a ColorPicker seeded through the descriptor's codec
  PASS  widgets: a color picker substitutes 1s for a missing or corrupt stored color
  PASS  widgets: the color codec is the descriptor's, so an array-storing host is not translated
  PASS  widgets: disabledIf grays the swatch out while its sibling toggle is on
  PASS  widgets: a function disabledIf disables every maker and is re-evaluated on refresh
  PASS  widgets: a path disabledIf disables every maker while that setting is on
  PASS  widgets: a row with no disabledIf never has its disabled state touched
  PASS  widgets: a disabledIf predicate that raises leaves the row drawn and enabled
  PASS  widgets: RenderRows opts.disabled disables every widget it draws, after-group ones included
  PASS  widgets: a disabled render's flag never leaks into a later render or into its refresh
  PASS  widgets: a render nested inside a disabled render inherits the disable
  PASS  widgets: an afterGroup hook that raises still propagates, and the flag is cleared
  PASS  widgets: OnValueConfirmed commits immediately — cancel must not wait on the throttle
  PASS  widgets: OnValueChanged throttles a drag to ONE timer and commits the LAST value
  PASS  widgets: a color drag does NOT refresh every panel
  PASS  widgets: every other maker's write DOES refresh every panel
  PASS  widgets: RenderField dispatches each schema type to its widget
  PASS  widgets: RenderField returns nil for an unrecognized type instead of erroring
  PASS  widgets: RenderField adds the widget to the parent it was given
  PASS  widgets: RenderSchema pairs widgets two-to-a-row inside full-width Flow groups
  PASS  widgets: a `solo` row is rendered alone on its own line
  PASS  widgets: a `solo` row flushes the row in progress rather than joining it
  PASS  widgets: a `skipRender` row is left to the host and never drawn
  PASS  widgets: RenderRows emits one Heading per group, in first-seen order
  PASS  widgets: a group's heading lands BELOW the previous group's tail row, not above it
  PASS  widgets: an afterGroup callback fires exactly once, after its group's last row
  PASS  widgets: an afterGroup callback runs with its group's tail row already on the page
  PASS  widgets: an afterGroup hook fires for a group's FIRST run only, when the group recurs
  PASS  widgets: a pairWith partner attaches to the named row, is one-shot, and stays 50/50
  PASS  widgets: a pairWith partner declines a row it would make three-wide
  PASS  widgets: RenderRows leaves the caller's afterGroup / pairWith tables intact
  PASS  widgets: RenderRows runs a layout pass at the end
  PASS  widgets: Section emits a full-width Heading and tracks the group
  PASS  widgets: ClearScroll releases the children AND resets ctx.refreshers
  PASS  widgets: ClearScroll reassigns ctx.refreshers rather than wiping it in place
  PASS  widgets: InlineButtonPair lays two inset buttons into one Flow row and pcalls the click
  PASS  widgets: InlineButtonPair tolerates a missing second spec
  PASS  widgets: InlineButtonPair reports a handler-less button once, and draws it anyway
  PASS  widgets: a number row carrying a values list renders as a Dropdown, not a Slider
  PASS  widgets: the numeric dropdown lists its entries with their own labels
  PASS  widgets: the numeric dropdown seeds the STORED number, not a stringified copy
  PASS  widgets: choosing an entry writes the number through the host's set
  PASS  widgets: a number row with NO values list still renders as a Slider
  PASS  widgets: a number row whose values function answers empty falls back to a Slider
  PASS  widgets: TextRow adds a full-width Label carrying the text
  PASS  widgets: TextRow left-justifies by default and honors an explicit justify
  PASS  widgets: TextRow applies a font object by NAME, and only when the global exists
  PASS  widgets: TextRow draws nothing and returns nil when there is no scroll to draw into
  PASS  widgets: BuildLandingPage draws the logo block at its declared size, then a spacer
  PASS  widgets: BuildLandingPage honors an explicit logoSize
  PASS  widgets: a logo whose widget has no backing frame costs the logo, not the page
  PASS  widgets: a POOLED frame gains ONE logo texture, and hides it when released
  PASS  widgets: a spec with no logo draws no logo block
  PASS  widgets: BuildLandingPage calls a notes FUNCTION at render time
  PASS  widgets: an empty one-liner skips the notes Label AND its spacer
  PASS  widgets: BuildLandingPage renders a heading and one row per section entry
  PASS  widgets: a section's rows are re-evaluated on every render
  PASS  widgets: a re-render clears the previous body instead of stacking a second copy
  PASS  widgets: the second landing heading gets a top spacer and the first does not
  PASS  widgets: the gap under a landing heading is emitted once, by Section
  PASS  widgets: BuildLandingPage tolerates a nil spec and an empty one
  PASS  widgets: the landing page's text rows carry the same justify guard TextRow owns
  PASS  widgets: a tabbed page draws ONLY the active group's rows
  PASS  widgets: a tabbed page draws no section heading -- the tab IS the heading
  PASS  widgets: a tabbed page falls back to the untabbed render when OptionsTabs.lua is absent
  PASS  widgets: the tab half draws its banner without the widget half's tooltip attacher
  PASS  widgets: an UNtabbed page still draws its headings
  PASS  widgets: clicking a tab clears the scroll and renders the new group
  PASS  widgets: the active tab survives a re-render, and heals when its group disappears
  PASS  widgets: a one-group page draws a ONE-TAB strip
  PASS  widgets: a page whose rows carry no group renders untabbed AND says so
  PASS  widgets: a host that omits print still sees NO_GROUPS in the chat frame
  PASS  widgets: with no AceGUI a tabbed page reports no tabs and draws nothing
  PASS  widgets: a subgroup draws a heading INSIDE a tab, where the group's own is suppressed
  PASS  widgets: a subgroup repeated under a SECOND group draws again
  PASS  widgets: the pending line is flushed before a subsection heading
  PASS  widgets: an UNTABBED page draws its group heading AND its subgroup headings
  PASS  widgets: a `wide` row renders at FULL width, alone, with the lines around it flushed
  PASS  widgets: `startsLine` flushes a half-full line so a declared pair cannot be split
  PASS  widgets: `startsLine` on a line that is already empty costs nothing
  PASS  widgets: InlineButtonPair with no right-hand button draws one, at the pair's width
  PASS  widgets: a string row with no values and no dialogControl prints once and still renders
  PASS  widgets: a values-backed row that is momentarily empty does NOT warn
  PASS  widgets: tab packing fills a row and wraps to the next
  PASS  widgets: a tab wider than the strip gets its own row rather than vanishing
  PASS  widgets: an empty tab list lays out as no rows at all
  PASS  widgets: __tabPlacement puts the first row below the banner, and wraps below that
  PASS  widgets: __tabPlacement accumulates x across a row
  PASS  widgets: __tabPlacement places every index exactly once
  PASS  widgets: __bannerBand widens a real height by the gap/rule/gap, and a zero one not at all
  PASS  widgets: __tabBand reserves the wrapped rows at their pitch, plus one full tab
  PASS  widgets: a tab is cut from the client's own tab atlases, active art on the selected one
  PASS  widgets: a tabbed page gets the client's content panel, drawn as two mirrored halves
  PASS  widgets: the content box is drawn WIDER than the content column it encloses
  PASS  widgets: wrapped rows are packed by the ART's height, so they sit flush
  PASS  widgets: a strip laid out before the canvas has a width re-wraps when the width arrives
  PASS  widgets: TabStrip draws one button per tab, marks the active one, and reserves the band
  PASS  widgets: clicking the ACTIVE tab does not re-fire onSelect
  PASS  widgets: a second TabStrip call replaces the first rather than stacking on it
  PASS  widgets: re-selecting the same tabs builds no second set of frames
  PASS  widgets: TabStrip refuses politely with no AceGUI and with no tabs
  PASS  widgets: PageBanner draws a seeded picker and reserves the banner band
  PASS  widgets: banner then strip reserve ONE band between them, not two
  PASS  widgets: banner then strip leave no overlap in the reserved band
  PASS  widgets: PageBanner refuses politely with no AceGUI and with no spec
  PASS  widgets: repeated strip renders do not grow the page-wide chrome ledger
  PASS  widgets: a wrapped strip's geometry is IDENTICAL for every value of the selection
  PASS  widgets: every tab's hit rect is inset by the same number the rows are packed by
  PASS  widgets: the pitch is measured once, off the INACTIVE family, and cached on success only
  PASS  widgets: PageHeader reserves the band, and the strip lands beneath it
  PASS  widgets: a page draws at most ONE chrome block -- the second replaces the first
  PASS  widgets: PageHeader without a divider draws the block and no rule
  PASS  widgets: a raising PageHeader builder costs the block, not the page
  PASS  widgets: PageHeader refuses politely with no spec and with no height
  PASS  widgets: two full renders of a banner page leave ONE Dropdown out, not two
  PASS  widgets: a header page after a banner page gives the banner's Dropdown back
  PASS  widgets: PageBanner's re-render from inside its own onSelect never hands itself back
  PASS  widgets: PageHeader hands the SAME frame back on every render of one page
  PASS  widgets: the divider texture is made once per page and hidden, never unparented
  PASS  widgets: SubTabStrip draws inside the host's frame and reports the height it took
  PASS  widgets: a second SubTabStrip call releases the first rather than stacking on it
  PASS  widgets: ClearScroll drains the sub-tab ledger before AceGUI pools the parent
  PASS  widgets: a wrapped SUB strip's geometry is invariant under the selected sub tab
  PASS  widgets: SubTabStrip refuses politely with no AceGUI, no parent and no tabs
  PASS  widgets: a host tab sits before the group it names and renders through its callback
  PASS  widgets: a host tab keyed by a group takes that group's place and is handed its rows
  PASS  widgets: a stale active tab heals to the first tab when only host tabs remain
  PASS  widgets: disabledFor draws the notice ABOVE the rows, and the rows disabled
  PASS  widgets: disabledFor false draws no notice and live rows; a raising one reads as enabled
  PASS  widgets: a host tab renders under the page's disable, and the flag never outlives it
  PASS  widgets: chrome is called once per render, after the strip and before the rows
  PASS  widgets: with OptionsTabs.lua absent RenderTabbedSchema takes opts and renders untabbed
  PASS  widgets: the tab half alone does not define RenderTabbedSchema over no flow engine
  PASS  widgets: PageBanner's action draws a Button whose click calls onClick
  PASS  widgets: PageBanner's action button is Released like its picker, never leaked
  PASS  widgets: PageBanner's action re-rendering from its own click never hands itself back
  PASS  widgets: PageBanner's action is refused in combat, like its picker
  PASS  IdInput suggestions: exact, then prefix, then a word, then anywhere; shorter first
  PASS  IdInput suggestions: one name's rows sort by rank, then by id
  PASS  IdInput suggestions: at most ten rows, then a line saying how many were left out
  PASS  IdInput suggestions: digits match ids by prefix; a name needs two letters
  PASS  IdInput suggestions: two letters means two characters, not two bytes
  PASS  IdInput suggestions: every rank is its own row, labeled, beside the others
  PASS  IdInput suggestions: a spell's rank is the client's subtext
  PASS  IdInput suggestions: a click adds that row's id once, through onAdd, and closes
  PASS  IdInput suggestions: Up and Down move the highlight, and Enter adds it
  PASS  IdInput suggestions: Enter with no row highlighted still refuses a shared name
  PASS  IdInput suggestions: a shared name the bags or the spellbook carry is refused, not one rank added
  PASS  IdInput suggestions: typing drops the highlight, so Enter never takes a row the text left
  PASS  IdInput suggestions: a shared name refused by Add, or before the pause, lists its ranks
  PASS  IdInput suggestions: a shared name refused after a lookup lists its ranks
  PASS  IdList suggestions: a pick reaches onAdd and rebuilds the list
  PASS  IdInput suggestions: Escape, focus loss, a hidden panel and a release close it
  PASS  IdInput suggestions: typing is debounced, and a released box's pending update is dropped
  PASS  IdInput suggestions: items in the bags and spells in the spellbook need no candidates
  PASS  IdInput suggestions: a missing source, a raising candidates() or no info costs nothing
  PASS  IdInput suggestions: an uncached candidate joins the list once it is named
  PASS  IdInput suggestions: a host kind with base = "item" shows each rank's tier and color
  PASS  IdInput suggestions: a based host kind's own false wins over its base
  PASS  IdInput suggestions: the id a based kind's resolve answers for a pick is the one added
  PASS  IdInput suggestions: a based kind built per render is collected with its view
  PASS  IdInput suggestions: a based host kind's resolve still decides what a pick adds
  PASS  IdInput suggestions: any library kind can be a base; its ranks and fields come with it
  PASS  IdInput suggestions: a based host kind is offered its base's client ids, ranked as the base ranks them
  PASS  IdInput suggestions: a host kind with no base is offered nothing of the client's
  PASS  IdInput suggestions: the shared-name check reads one source through a based kind and its base
  PASS  IdInput suggestions: one dropdown per instance, whatever the renders
  PASS  IdInput suggestions: a box pooled into a second render is hooked once
  PASS  IdInput suggestions: a box pooled into another instance wakes no list of the first's
  PASS  IdInput suggestions: a released box lets its render's index go
  PASS  IdInput suggestions: Enter in a box that no longer owns the dropdown submits its text
  PASS  IdInput suggestions: a box that left before the pause shows nothing
  PASS  IdInput suggestions: focus lost to the dropdown itself goes back to the box
  PASS  IdInput suggestions: one render names at most 2000 ids
  PASS  IdInput suggestions: a raising info costs that id's row, not the list
  PASS  IdInput suggestions: the dropdown is as wide as the box looks
  PASS  IdInput suggestions: a host kind's suggestTag is appended after the id
  PASS  IdList removeStyle icon: an X leads every line, then the name; no Remove button and no checkbox
  PASS  IdList removeStyle icon: a click on the X calls onRemove and rebuilds the list
  PASS  IdList removeStyle icon: the X's tooltip is the remove string, a host's override honored
  PASS  IdList without removeStyle draws exactly as before: the name, then Remove or a checkbox
  PASS  IdList removeStyle icon: drawn disabled, the X is disabled
  PASS  IdList removeStyle icon: the X lights its whole hit area, not just the art inside it
  PASS  IdList removeStyle icon: the highlight goes back onto the art when AceGUI takes the X back
  PASS  IdList removeStyle icon: an AceGUI whose Icon has no textures still draws the X
  PASS  switched: only the subsection the selector names is drawn, its heading with it
  PASS  switched: equals may list several values; the row shows for any of them
  PASS  switched: a group's afterGroup hook fires after its last DRAWN row, once
  PASS  switched: changing the selector re-renders the page once, on the next frame
  PASS  switched: a write from anywhere (a slash set, a reset) re-renders too, once per change
  PASS  switched: a selector that cannot be read shows its rows rather than losing them
  PASS  switched: rows without shownWhen render as before, and no selector watcher is added
  PASS  switched: a bound (path-less) selector reads and is watched through its record
  PASS  switched: two selectors changing in one frame cost one re-render
  PASS  combat: a page shown in combat is covered and not drawn, and the window is left alone
  PASS  combat: the cover is built out of combat, hidden, and takes the mouse and the wheel
  PASS  combat: REGEN_DISABLED covers an open tabbed page above its tab strip
  PASS  combat: a widget write is refused and the widget put back
  PASS  combat: the notice comes back once per combat, not once per session
  PASS  combat: the page's Defaults — header button and footer control — are refused
  PASS  combat: a library button's click is refused
  PASS  combat: a session checkbox is refused and put back
  PASS  combat: a color commit is refused and the swatch put back
  PASS  combat: a page banner's selection is refused and the dropdown put back
  PASS  combat: a tab click is refused and the page stays on its tab
  PASS  combat: SelectTab refuses in combat
  PASS  combat: a structural refresh in combat waits; the page renders when combat ends
  PASS  combat: a page first shown in combat renders when combat ends, and is never re-opened
  PASS  combat: a clean open page runs its refreshers when combat ends, not its renderer
  PASS  combat: a hidden page is left for its next show
  PASS  combat: REGEN_DISABLED closes the library's own dropdown pullout on an open page
  PASS  combat: another addon's focused widget is left alone
  PASS  combat: out of combat nothing changes
  PASS  combat: one event frame for the library, dispatching through lib at call time
  PASS  combat: the lock predicate is the flag or the client's lockdown
  PASS  combat: with no page shown the library holds no registration and shows no frame
  PASS  combat: a page's show registers the events and the last hide unregisters them
  PASS  combat: a page hidden in combat drops its cover and lets go of the events
  PASS  combat: a page shown mid-combat is locked off InCombatLockdown alone
  PASS  combat: a page hidden without OnHide is let go at the next combat edge
  PASS  combat: a registration an older copy left is dropped when nothing is shown
  PASS  combat: CreateOptionsPanel in combat registers nothing and waits for REGEN_ENABLED
  PASS  combat: the end of combat registers the parked panel once and lets go of the event
  PASS  combat: a second CreateOptionsPanel while parked is a no-op
  PASS  combat: an event other than REGEN_ENABLED leaves the park armed
  PASS  combat: OpenOptionsPanel answers false in combat, true when opened, nil with no category
  PASS  compose: the instance carries every composer and every published constant
  PASS  compose: FontGroup emits the six canonical leaves in the canonical order
  PASS  compose: BorderGroup emits the four mandated leaves, and the toggle only when asked
  PASS  compose: BarGroup emits texture, opacity, color, companion -- in that layout
  PASS  compose: ColorPair emits exactly two rows, and names the companion after the swatch
  PASS  compose: FontGroup's font row answers a populated list, not a second closure
  PASS  compose: BorderGroup's border-style row answers a populated list
  PASS  compose: BarGroup's bar-texture row answers a populated list
  PASS  compose: a host whose own LSMValues returns a TABLE lands a frozen list, which is the breach
  PASS  compose: every color row is immediately followed by its companion, and starts a line
  PASS  compose: no composed row anywhere carries disabledIf
  PASS  compose: the class-color SOURCE is stamped on both halves of every pair
  PASS  compose: prefix composes the path, and page/group/subgroup reach every row
  PASS  compose: order starts where the caller said and steps by ten
  PASS  compose: keys, labels and defaults override without changing what the block IS
  PASS  compose: omit removes a row and leaves the survivors in the same relative order
  PASS  compose: extra rows are appended AFTER the mandated block, never interleaved
  PASS  compose: a composer never writes to the spec it was handed
  PASS  compose: MasterControls emits the six canonical rows and defaults its own group
  PASS  compose: testModePath adds one session-only Test mode row, on its own line after the console
  PASS  compose: minimapPath adds a stored Minimap button row, opening the line Test mode pairs on
  PASS  compose: Minimap button and Test mode render as ONE line, minimap first
  PASS  compose: either row alone still opens its own line
  PASS  compose: the debug console's path is verbatim and outside the block's prefix
  PASS  compose: frameless drops EXACTLY the four frame-only controls and nothing else
  PASS  compose: a frameless addon's lead button shares the pair with Reset all settings
  PASS  compose: a FRAMED addon's lead button takes its own row above the full pair
  PASS  compose: the tail draws the two resets as the tab's closing button pair
  PASS  compose: a path-keyed call emits byte-for-byte what compose minor 3 emitted
  PASS  compose: the bind arm emits the same rows as the path arm, with the binding in place of path
  PASS  compose: a bound row's get and set reach the bind with the record field and the row
  PASS  compose: bind.record is enough to read, and an extra's own path is left alone
  PASS  compose: a bind with no setter, or with nothing to read, is refused when the block is composed
  PASS  compose: every maker reads a bound row through get and writes it through set, never the store
  PASS  compose: a bound row's refresher re-reads the record, so a write elsewhere repaints it
  PASS  compose: a row WITH a path is read through the descriptor even when it carries get and set
  PASS  compose: PanelMaster's three record-backed groups compose, in the order the editor draws them
  PASS  compose: a PanelMaster block writes through the registry and repaints off the live record
  PASS  compose: a bound row takes its pairWith partner, keyed by its field
  PASS  compose: disabledIf on a bound row reads the record through the bind, not the settings store
  PASS  compose: with no resetProfile the Reset all tooltip keeps its minor-4 wording, byte for byte
  PASS  compose: with resetProfile the Reset all tooltip says it resets the current profile only
  PASS  compose: with resetProfile and profilesPage the tooltip names Profiles -> Reset Profile
  PASS  compose: the descriptor moves the Reset all tooltip and nothing else Master controls draws
  PASS  compose: a shell that hands __AttachCompose no descriptor keeps the minor-4 tooltip
  PASS  throttle: a nil-returning scheduleTimer still gets ONE slider timer per window
  PASS  throttle: a nil-returning scheduleTimer still gets ONE color timer per window
  PASS  throttle: the window re-arms after it fires, for a nil-returning host
  PASS  throttle: a host whose timer answers a handle is unchanged
  PASS  lib: registers under its major with a schema and a default ring
  PASS  lib: New requires a name, an sv global and a lifecycle latch
  PASS  lib: New rejects a bucket entry with no key, in the library's own words
  PASS  lib: a ring of zero is clamped to one, not left to empty itself
  PASS  lib: a negative ring is clamped too
  PASS  lib: two instances share no state
  PASS  lib: bucket order and nesting come from the descriptor
  PASS  lib: the capture gate starts off
  PASS  lib: Note accumulates calls, total and max
  PASS  lib: Note tracks unrelated buckets independently
  PASS  lib: Note takes the observed parent, and defaults to claiming nothing
  PASS  lib: two different observed parents for one bucket are flagged, not overwritten
  PASS  lib: Note with no key names the caller instead of raising a table-index error
  PASS  lib: Reset clears every bucket and both fps arms
  PASS  lib: Open records nothing while the probe is off
  PASS  lib: Close with no matching open slot is a silent no-op
  PASS  lib: Open with no key names the caller instead of raising a table-index error
  PASS  lib: a real bracket records its elapsed ms to the named bucket
  PASS  lib: Open/Close feed the same buckets P.Note does
  PASS  lib: a bracket opened inside another records the enclosing key as its observed parent
  PASS  lib: an exit that forgot its Close is discarded, not credited with a later bracket's time
  PASS  lib: a leaked Open in window A does not parent a bracket in window B
  PASS  lib: EncodeJSON emits object keys in sorted order
  PASS  lib: EncodeJSON renders integral numbers without a decimal point
  PASS  lib: EncodeJSON renders fractional numbers to four places
  PASS  lib: EncodeJSON escapes quotes, backslashes and control characters
  PASS  lib: EncodeJSON emits arrays for sequence tables
  PASS  lib: EncodeJSON emits an empty table as an object
  PASS  lib: EncodeJSON coerces non-finite numbers rather than emitting invalid JSON
  PASS  lib: EncodeJSON encodes a whole record, not just its buckets
  PASS  lib: BuildRecord carries schema, source and label
  PASS  lib: BuildRecord derives avgFps and msPerFrame from the arms
  PASS  lib: BuildRecord reports zero delta when only one arm was sampled
  PASS  lib: BuildRecord computes the delta when both arms were sampled
  PASS  lib: BuildRecord snapshots buckets rather than aliasing them
  PASS  lib: a record names the addon that produced it
  PASS  lib: a nested bucket carries its parent into the record
  PASS  lib: a record stamps the host's interface version and the capture time
  PASS  lib: the record's context names the character's class
  PASS  lib: a canceled run takes its context stamp with it
  PASS  lib: a record built before Start has no context at all
  PASS  lib: a Note key the descriptor never declared still lands in the record
  PASS  lib: Save creates the perf global and appends the run
  PASS  lib: Save stamps the schema on the store
  PASS  lib: Save trims the ring to ringMax, dropping the oldest
  PASS  lib: Save traces a retention prune to the host log, and only when it prunes
  PASS  lib: a ring written under another schema is discarded, not converted
  PASS  lib: FormatReport emits its sections in reading order
  PASS  lib: FormatReport marks an unsampled arm rather than printing zeros
  PASS  lib: FormatReport prints both arms and the delta when both ran
  PASS  lib: FormatReport derives ms/s from the active seconds only
  PASS  lib: FormatReport warns that buckets nest
  PASS  lib: FormatReport omits buckets that never fired
  PASS  lib: FormatReport indents a nested bucket under its parent
  PASS  lib: a declared parent nobody supplied is reported as declared, NOT as observed
  PASS  lib: a supplied parent is reported as observed
  PASS  lib: a parent observed somewhere other than the declared one is reported as the defect it is
  PASS  lib: observed containment travels into the record beside the declared value
  PASS  lib: a flat bucket set gets no nesting footer
  PASS  lib: Context captures character, spec, zone and group
  PASS  lib: Context reports solo when ungrouped
  PASS  lib: Context takes the namespaced spec reader before the bare global
  PASS  lib: Context reports party size and instance type
  PASS  lib: Context reports raid size
  PASS  lib: ContextLines folds the sub-zone into the location
  PASS  lib: ContextLines omits an empty sub-zone cleanly
  PASS  lib: ContextLines tolerates a record with no context
  PASS  lib: a host passing only the three required fields gets working defaults
  PASS  perf: an L whose metatable synthesizes every key does NOT mask the module's strings
  PASS  perf: a step label is never its own SCREAMING_SNAKE_CASE key
  PASS  perf: a REAL entry in an L that also has a fallback still overrides
  PASS  lib: suspend returns false when already suspended
  PASS  lib: resume returns false when not suspended
  PASS  lib: the suspended state is session-only, never persisted
  PASS  lib: starting an experiment logs it
  PASS  lib: stopping an experiment logs both arm durations
  PASS  lib: suspend and resume are logged
  PASS  lib: a no-op suspend or resume logs nothing
  PASS  lib: nothing is logged when no run is happening
  PASS  lib: an armed window samples nothing until combat begins
  PASS  lib: a window opens on combat and accumulates
  PASS  lib: a window closes when combat ends and stops accumulating
  PASS  lib: the walk between windows is never measured
  PASS  lib: measure b suspends the addon and measure a resumes it
  PASS  lib: window B still samples while the addon is suspended
  PASS  lib: re-arming a window zeroes it rather than averaging in
  PASS  lib: arming a window mid-combat closes the one already open
  PASS  lib: Measure is rejected outside an experiment
  PASS  lib: Measure rejects an unknown window token
  PASS  lib: Measure accepts either case
  PASS  lib: Stop closes an open window rather than discarding it
  PASS  lib: Stop detaches the sampler so an idle client pays nothing
  PASS  lib: the sampler ignores ticks once the experiment is over
  PASS  lib: Stop deliberately leaves a suspended host suspended
  PASS  lib: two completed windows produce a delta
  PASS  lib: recording start and end are announced to chat AND the debug log
  PASS  lib: the end announcement carries the duration and frame rate
  PASS  lib: the console log is plain text, free of color escapes
  PASS  lib: experiments are named A and B, never active/suspended
  PASS  lib: the run start is logged with its context
  PASS  lib: arming logs which experiment and whether the addon is suspended
  PASS  lib: measure b calls the host's suspend, measure a its resume
  PASS  lib: canceling a suspended run restores the host
  PASS  lib: the stopwatch is driven per window
  PASS  latch: Suspend takes the 'perf' hold and Resume gives it back
  PASS  latch: p.suspended reads the latch rather than a copy of it
  PASS  latch: assigning p.suspended raises rather than shadowing the latch
  PASS  latch: releasing the perf hold does NOT resurrect an addon `disabled` still holds down
  PASS  latch: the resume log line follows what actually happened
  PASS  latch: finish releases the hold before saving, and says which of the two happened
  PASS  latch: the perf hold is session-only and reaches no SavedVariables
  PASS  lib: before a run Start is the one offered step
  PASS  lib: Start reads done while a run is in flight
  PASS  lib: Start is offered again once a run has finished
  PASS  lib: starting a run makes exactly Measure A ready
  PASS  lib: an armed or recording experiment reads busy, not ready
  PASS  lib: completing A unlocks B and nothing else
  PASS  lib: completing B unlocks Finish
  PASS  lib: finishing unlocks Report
  PASS  lib: exactly one step is ready at any point in a run
  PASS  lib: re-arming a completed experiment sends it back to busy
  PASS  lib: re-arming Experiment B relocks Finish
  PASS  lib: a step state is always one of the state words, never a value the run holds
  PASS  lib: a window that caught no frames still counts as completed
  PASS  lib: Reset clears completion, so a fresh run starts from step one
  PASS  lib: the panel renders every step and tracks their states
  PASS  lib: a locked panel button refuses to act when clicked
  PASS  lib: the panel repaints itself on every state transition
  PASS  lib: every step row carries a status dot, drawn not glyphed
  PASS  lib: labels are plain text with no decoration baked in
  PASS  lib: cancel is offered throughout a run and nowhere else
  PASS  lib: cancel has its own state, so it never reads as the next step
  PASS  lib: canceling discards the run without saving it
  PASS  lib: canceling restores a suspended addon
  PASS  lib: canceling mid-recording does not announce the experiment as ended
  PASS  lib: canceling detaches the sampler
  PASS  lib: cancel returns false when there is nothing to cancel
  PASS  lib: a canceled run leaves the next one clean
  PASS  lib: every row shows its slash command
  PASS  lib: cancel stays clickable while a run is mid-experiment
  PASS  lib: cancel is not clickable once the run is finished
  PASS  lib: hiding the panel never touches the run
  PASS  lib: Toggle flips visibility both ways
  PASS  lib: decorate is handed the frame and a way to close it
  PASS  lib: a host that passes no decorate still gets a working panel
  PASS  lib: the fallback close button is told which addon is asking
  PASS  lib: an addonName in the descriptor wins over the name
  PASS  lib: a host that decorates gets no close button from the library
  PASS  lib: report stays clickable after use, but reads as done
  PASS  lib: marking a review action twice is a no-op
  PASS  lib: MarkReviewed ignores keys that are not review actions
  PASS  lib: a fresh run clears the review marks
  PASS  lib: the panel titles itself like the debug console
  PASS  lib: a locale table filled in after New still reaches the rows
  PASS  lib: every step label names what it acts on
  PASS  lib: a panel-less instance answers STEPS, PanelStateOf and PanelIsActionable safely
  PASS  cmd: OnCommand always returns a line table, never nil
  PASS  cmd: start begins a run and shows the panel
  PASS  cmd: a label is appended to the timestamp, never replaces it
  PASS  cmd: measure reports which window armed and whether the host is suspended
  PASS  cmd: measure outside a run tells you to start one
  PASS  cmd: measure rejects an unknown window token
  PASS  cmd: finish resumes the host before it saves
  PASS  cmd: finish prints no report
  PASS  cmd: report writes the summary to the log sink and opens it
  PASS  cmd: report writes the summary AND the JSON, in that order
  PASS  cmd: dump is no longer a verb of its own
  PASS  cmd: cancel refuses when there is nothing to cancel
  PASS  cmd: show, hide and toggle drive the panel and nothing else
  PASS  cmd: a bare command reports the phase and prints the usage
  PASS  cmd: usage never hard-codes a slash prefix
  PASS  cmd: usage never leaves a bare pipe where the client reads an escape
  PASS  cmd: usage rows use the library's own row formatter
  PASS  cmd: clicking a ready panel row takes the same path as typing it
  PASS  cmd: a panel click prints exactly what typing the command prints
  PASS  cmd: clicking a locked panel row does nothing
  PASS  iso: two instances create separate sampler frames
  PASS  iso: driving one instance's sampler accumulates into that instance alone
  PASS  iso: an instance's sampler is detached without touching the other's
  PASS  iso: a dormant Open/Close bracket allocates nothing and records nothing
  PASS  iso: an active Open/Close bracket reuses its slots instead of allocating one per open
  PASS  iso: armed, recording and label stay raw fields across a window and a cancel
  PASS  iso: two instances create separate panel frames
  PASS  iso: each panel renders its own host's state and its own slash prefix
  PASS  iso: clicking one host's panel drives that host only
  PASS  iso: a newer probe loading second brings its own panel with it
  PASS  iso: an older copy loading second replaces neither half
  PASS  iso: a higher panel minor over the same probe still wins
  PASS  loader: a cached chunk gives each instance its own global namespace
  PASS  loader: a chunk read once is not re-read
  PASS  loader: uncache(path) forces the next load to re-read
  PASS  loader: uncache() with no argument empties the whole cache
  PASS  loader: a missing file raises, and names the path
  PASS  loader: a file that fails to compile stays an error until it is fixed
  PASS  parallel: every suite lands in exactly one shard, for every split
  PASS  parallel: the shards concatenate back into the original order
  PASS  parallel: the split is balanced to within one suite
  PASS  parallel: more shards than suites yields empty shards, not overlapping ones
  PASS  kit guard: a guarded process sees its depth one deeper and its own arguments unchanged
  PASS  kit guard: a runner that starts itself stops at the depth limit instead of forking forever
  PASS  kit guard: the guarded child's exit code is the run's exit code
  PASS  kit guard: a run past its wall-clock limit is stopped with 124 and says so
  PASS  kit guard: KA0S_KIT_GUARD=off runs the process as it was started
  PASS  kit guard: exit statuses normalize across Lua 5.1 and 5.2+
  PASS  kit limits: a case's CPU ceiling stops a loop, even one that swallows the first error
  PASS  kit limits: a bounded call hands back every result, nils included
  PASS  kit limits: --jobs is capped by memory, never below one, and unchanged when memory is unknown
  PASS  kit limits: a host path in a suite is caught, and a WoW path is not
  PASS  kit limits: the heap budget names the case that crossed it
  PASS  kit limits: the leak gate counts what is still held, not garbage waiting to be swept
  PASS  kit: assertErrorMatches passes on a raise that carries the needle, and returns the error
  PASS  kit: assertErrorMatches fails when fn raises something else, naming both strings
  PASS  kit: assertErrorMatches fails when fn does not raise
  PASS  kit: assertLibraryConstant passes on the live bytes, and on a dotted member path
  PASS  kit: assertLibraryConstant fails on a one-byte difference, naming both strings
  PASS  kit: assertLibraryConstant fails clearly on an unknown major or member
  PASS  kit: assertLibraryConstant falls back to LibStub when the source maps the name to an instance
  PASS  mock: a frame that was never armed answers zero, dressed or not
  PASS  mock: __setGeom is the opt-in, and the only thing that arms a frame
  PASS  mock: an armed frame takes its height from the published atlas table
  PASS  mock: SetAtlas records the name whether or not a size was asked for
  PASS  mock: an atlas the table does not publish leaves geometry alone
  PASS  mock: the selected and unselected tab atlases are published at different heights
  PASS  mock: a new frame is shown until hidden, as in the client
  PASS  mock: AceGUI:Release takes a widget back: flagged, frame hidden, recorded in order
  PASS  mock: AceGUI:Release fires OnRelease, then drops the children and the callbacks
  PASS  mock: a Release reached from the widget's own OnRelease is ignored, as AceGUI's guard ignores it
  PASS  mock: AceGUI:Release(nil) raises, as the real one does
  PASS  mock: releasing a widget twice raises, as AceGUI's delWidget does
  PASS  mock: widget:Release() is AceGUI:Release(widget), as WidgetBase.Release is
  PASS  mock: AceGUI:Release wipes userdata in place and the size fields, as the real one does
  PASS  mock: an AceEvent embed records game events the way the NewAddon target does
  PASS  mock: the embed and the NewAddon target share one event implementation
  PASS  mock: UnregisterAllEvents leaves an embed's message registrations alone
  PASS  mock: embedding a target a second time keeps what it had registered
  PASS  mock: a target reused by a later mock build starts with nothing registered
  PASS  mock: a dropped mock build takes every target embedded in it with it
  PASS  mock: RegisterEvent refuses what CallbackHandler refuses
  PASS  mock: NewAddon clobbers a custom Printf exactly as it clobbers Print
  PASS  mock: the console mixins print as AceConsole's do, bare, as methods and to a given frame
  PASS  mock: a bare Printf with nothing after the format string raises, as format() does
  PASS  mock: the id lookups are absent until a harness installs them
  PASS  mock: installing the id lookups fills only what a harness has not defined
  PASS  mock: a spell record answers by id and by name, the name in any case
  PASS  mock: an uncached item keeps its icon and hides its name until it loads
  PASS  mock: an item record answers its quality by id and by link, and none while uncached
  PASS  mock: a currency record answers by id, and clearIdRecords empties every kind
  PASS  mock: the suggestion sources answer what a suite seeds -- bags, spellbook, tiers, subtext
  PASS  mock: installIdSuggestions gives an AceGUI EditBox its editbox frame, and nothing else
  PASS  mock: an AceGUI widget answers GetText and records SetType and DisableButton
  PASS  ace: NewAddon with a name embeds exactly the libraries it lists
  PASS  ace: NewAddon refuses what AceAddon refuses
  PASS  ace: NewAddon(name) builds the object itself, named and printable as its name
  PASS  ace: a table with no name keeps revision 16's NewAddon, stamps and all
  PASS  ace: NewModule makes a named child addon, in creation order
  PASS  ace: a module takes its prototype, default libraries and default state
  PASS  ace: PLAYER_LOGIN initializes everything queued, then enables the addon before its modules
  PASS  ace: ADDON_LOADED initializes; enabling waits for the login
  PASS  ace: a module created disabled is skipped by the cascade and enabled on demand
  PASS  ace: Disable runs OnDisable, then disables every module, and Enable brings them back
  PASS  ace: Enable on an addon still queued for initialization only records the state
  PASS  ace: disabling an addon unregisters its events and messages and cancels its timers
  PASS  ace: an OnEnable that raises costs only itself, and the cascade reports it afterwards
  PASS  ace: RegisterMessage dispatches a string method, a default method and the optional arg
  PASS  ace: RegisterMessage refuses what CallbackHandler refuses
  PASS  ace: UnregisterAllMessages drops this target's messages and nobody else's
  PASS  ace: the library's own SendMessage fans out, and the registry holds a function as given
  PASS  ace: a registration made while a message is being sent waits for the send to finish
  PASS  ace: __fireEvent dispatches a game event the way CallbackHandler does
  PASS  ace: an event the client does not know raises on its first registration
  PASS  ace: ScheduleTimer queues a timer __fireTimers runs, with its arguments
  PASS  ace: ScheduleTimer refuses what AceTimer refuses
  PASS  ace: CancelTimer is honored, answered, and not counted as a run
  PASS  ace: a repeating timer fires once per pass until it is canceled, even from inside itself
  PASS  ace: CancelAllTimers cancels this object's timers and nobody else's
  PASS  ace: TimeLeft reads the clock; a short delay is floored at AceTimer's 0.01
  PASS  ace: a C_Timer.NewTimer handle's Cancel is honored too
  PASS  ace: RegisterChatCommand records the command and dispatches it as the client would
  PASS  ace: where the environment models SlashCmdList, RegisterChatCommand writes the client's globals
  PASS  ace: every Embed works when a consumer's wrapper calls it with its own table as self
  PASS  ace: a repeating timer keeps its delay and its TimeLeft when the test never moves the clock
  PASS  ace: only a lone table argument takes the no-name path; everything else is validated
  PASS  ace: the no-name path's CancelTimer is honored by __fireTimers
  PASS  ace: a message handler that raises costs only itself, and the send reports it afterwards
  PASS  ace: ADDON_LOADED after the login enables a load-on-demand addon, reading IsLoggedIn at call time
  PASS  ace: the AceEvent library carries the message registration API, as CallbackHandler publishes it
  PASS  ace: AceGUI's layout registry and version table carry their real names
  PASS  ace: AceDB's OnProfileCopied carries the SOURCE profile's key, as AceDB-3.0 fires it
  PASS  mock_ace: AceDB's ResetProfile fires OnProfileReset with the database alone
  PASS  record: a fresh build has registered nothing
  PASS  record: a raw frame:RegisterEvent is recorded, by frame and by name
  PASS  record: entries are REMOVED on unregister and on UnregisterAllEvents
  PASS  record: a per-unit registration is one row PER UNIT TOKEN
  PASS  record: AceEvent events and messages are recorded under their own kinds
  PASS  record: an embedded FRAME loses its raw and per-unit rows to UnregisterAllEvents too
  PASS  record: the survey comes back in the same order twice
  PASS  record: a bucket registration is a registration, and it is removable
  PASS  record: a bucket fires on its interval, through the one timer queue
  PASS  record: a bucket unregistered before its tick does NOT call back
  PASS  record: __fire reaches the LIVE set only
  PASS  record: __fireUnconditional reaches a target whose registration is gone
  PASS  record: __fireUnconditional drives an AceEvent handler too
  PASS  record: the timer queue is both the pending array and the live set
  PASS  record: a canceled ticker leaves the live set
  PASS  record: a repeating AceTimer stays live across a tick, and leaves on cancel
  PASS  record: a frame carrying an OnUpdate is live until the script is cleared
  PASS  record: __shownFrames answers what is on screen, in creation order
  PASS  record: a frame an addon's own mock built with __stubFrame is surveyed too
  PASS  record: every line that reached the chat frame is recorded, and resettable
  PASS  record: __printed hands back a copy, not the live log
  PASS  record: a host printer that bypasses the chat frame can still be recorded
  PASS  record: a write to the profile is reported by path and value
  PASS  record: an addon that writes nothing reports zero writes
  PASS  record: clearing a key counts as a write
  PASS  record: a global SavedVariables table can be watched explicitly
  PASS  record: two databases are told apart in the report
  PASS  record: CopyProfile onto the active profile raises AceDB's own message
  PASS  record: CopyProfile from a missing profile raises unless silent
  PASS  record: CopyProfile(missing, true) is silent, and resets the active profile as AceDB does
  PASS  record: DeleteProfile on the active profile raises AceDB's own message
  PASS  record: DeleteProfile on a missing profile raises unless silent
  PASS  record: DeleteProfile(missing, true) is silent, and a real delete still deletes
  PASS  record: SetProfile strips values equal to their defaults from the OUTGOING profile
  PASS  record: SetProfile keeps a value that differs from its default
  PASS  record: __aceguiLive counts a Create per type and gives it back on Release
  PASS  record: __aceguiLive ignores a Release it never saw created, and a registered type counts
  PASS  events: an EventRegistry callback appears in __registrations as kind 'callback'
  PASS  events: UnregisterCallback removes the callback from __registrations
  PASS  events: one callback per (event, owner) -- a second registration replaces the first
  PASS  events: TriggerEvent reaches a live callback as func(owner, ...) and skips a removed one
  PASS  events: RegisterCallback refuses what CallbackRegistry refuses
  PASS  events: a callback registered with no owner gets a generated numeric owner
  PASS  events: callback rows follow the other kinds, ordered by event then registration
  PASS  events: each build has its own EventRegistry
  PASS  events: frame:RegisterEvent raises on a name in __badEvents, and records nothing
  PASS  events: frame:RegisterUnitEvent raises on a name in __badEvents, and records nothing
  PASS  events: the frame path reads __badEvents at call time
  PASS  events: the frame raise names the caller, not the kit
  PASS  events: C_EventUtils.IsEventValid answers false for a bad name and true otherwise
  PASS  events: a suite may remove C_EventUtils to model an older client
  PASS  parity: a stub carrying every public member of a live major passes
  PASS  parity: a stub missing one member fails and names it
  PASS  parity: every divergence lands in one message
  PASS  parity: a member that is a function live and something else degraded is reported
  PASS  parity: a member left out on purpose is named as data, not omitted in silence
  PASS  parity: a major the surface source cannot resolve fails rather than passes
  PASS  parity: with no surface source registered the gate fails rather than passes
  PASS  versioning: every declared major is actually registered
  PASS  versioning: every file in every major registers its live version
  PASS  versioning: no file registers under a major it does not belong to
  PASS  versioning: every registered version is a positive integer
  PASS  versioning: file basenames are unique across every major
  PASS  versioning: the changelog accounts for the version every file is at
  PASS  versioning: every paired secondary file records which primary it attached to
  PASS  versioning: every major's live version has its API document on disk
  PASS  versioning: every major's published member manifest matches its live surface
  PASS  kitsync: Kit.VERSION is a positive integer and reaches the exposed table
  PASS  kitsync: the kit revision has an API document
  PASS  kitsync: the kit revision is indexed in docs/api/README.md as the one Current revision
  PASS  kitsync: the runner is mode 100755 in the git index, in BOTH copies
  PASS  kitsync: testkit/ and tests/_kit/ hold the same set of files
  PASS  kitsync: testkit/asserts.lua and testkit/prose_lists.lua exist in both testkit/ and tests/_kit/
  PASS  kitsync: every kit file is byte-identical in testkit/ and tests/_kit/, README included
  PASS  kitsync: vendor_sync checks the runner's recorded mode, and this repo's copy passes
  PASS  kitsync: the runner-mode case fails on a path the index records 100644
  PASS  kitsync: the runner-mode case fails on a path the index does not track
  PASS  kitsync: the runner-mode case skips, with a reason, where there is no work tree
  PASS  kitsync: the runner-mode case skips, with a reason, where io.popen is unavailable
  PASS  prose: no British spelling in the shipped library, the shipped kit or the store roots
  PASS  prose: the live API document of a major is its highest version key, compared numerically
  PASS  prose: no British spelling in the tests, the live docs or the artwork tools
  PASS  prose: the ASCII gate scans LibKa0s/ and not testkit/
  PASS  prose: no non-ASCII byte reaches a player, the em dash excepted
  PASS  prose: no retired §N.M section reference in the shipped library or the shipped kit
  PASS  every deviation id the register cites is assigned by a bundle in docs/audits/
  PASS  a kit suite declared with its directory is covered
  PASS  a bare declaration does not cover the kit's file of the same name
  PASS  an unreferenced kit suite with no local twin is the same hole
  PASS  a bare entry whose file ships only in the kit is told which directory it wants
  PASS  a recorded decline in docs/ARCHITECTURE.md is a skip, not a hole
  PASS  a recorded decline in the root CLAUDE.md is read too
  PASS  a decline is reported once however often the inventory is asserted
  PASS  a row keyed to the rule but silent about the suite grants nothing
  PASS  a row that only mentions the repo's own copy grants nothing
  PASS  a row naming the suite but keyed to another rule grants nothing
  PASS  a row in a subsection of the register is not a deviation row
  PASS  `localization-5` and `localization-§5` are the same key
  PASS  the rule each kit gate serves is written down
  PASS  no kit string literal cites a section without the section sign
  PASS  a repository with no register at all is not accidentally declined
  PASS  a listed suite that is not on disk raises, naming the path and the position
  PASS  a listed suite that is absent here but ships in the kit is told so
  PASS  a `pending` entry with no file registers a skip carrying its reason
  PASS  a `pending` entry whose file exists raises
  PASS  the kit is revision 26
  PASS  a `tests/_kit/` declaration covers the kit against a runner dir of `./tests/`
  PASS  a real shadow is still reported when the runner dir is spelled `./tests/`
  PASS  a `./` segment inside the runner dir does not fork the pair key
  PASS  a doubled slash and a missing trailing slash are the same directory
  PASS  the directory normalizer folds only the spellings it claims to
  PASS  a relative declaration is not a collision under an absolute runner dir
  PASS  an absolute declaration is not a collision under a relative runner dir
  PASS  a mixed suites list survives being invoked from another working directory
  PASS  the remedy names a `dir` a suites list can actually carry
  PASS  the two sides are resolved against each other, and only where they differ
  PASS  a remedy under a relative runner dir is printed exactly as it resolved
  PASS  characterization: a plain kit hole is reported word for word
  PASS  characterization: a kit suite with no rule row names the fallback rule
  PASS  characterization: a collision is reported word for word
  PASS  characterization: of several shadows, the last declared is the one named
  PASS  characterization: a decline's name and reason, word for word
  PASS  characterization: a decline over a collision names the file that runs instead
  PASS  characterization: a decline with an empty rule cell, and a reason clipped at 200 bytes
  PASS  eol repo kind: a root .toc is the evidence even when a nested one sorts first
  PASS  eol repo kind: with no root .toc, the first nested one in tracked order
  PASS  eol repo kind: a .toc outranks a libs/ tree
  PASS  eol repo kind: a libs/ tree with no .toc, first path in tracked order
  PASS  eol repo kind: a libs/ tree outranks a library payload folder
  PASS  eol repo kind: only a top-level libs/ counts
  PASS  eol repo kind: a payload folder: its own aggregate XML with Lua beside it
  PASS  eol repo kind: Lua anywhere under the payload folder counts
  PASS  eol repo kind: of two payload folders, the first in tracked order
  PASS  eol repo kind: an aggregate XML with no Lua in its folder is not a payload
  PASS  eol repo kind: an XML not named after its folder is not a payload
  PASS  eol repo kind: an aggregate XML below the folder's top is not a payload
  PASS  eol lone CR: a line ending \r\r\n fails, naming path:line
  PASS  eol lone CR: every lone CR is named, including one at end of file
  PASS  eol lone CR: a file with a NUL byte is skipped
  PASS  eol lone CR: clean CRLF and clean LF files pass
  PASS  prose scan-back: a store-root perf-analysis README is read though its folder is skipped
  PASS  prose scan-back: a store-root automated-tests README is read
  PASS  prose scan-back: a store-root automated-tests RESULTS.md is read
  PASS  prose scan-back: a dated perf-analysis bundle stays skipped
  PASS  prose scan-back: a dated automated-tests bundle stays skipped
  PASS  prose scan-back: a store-root name one folder down is a bundle file
  PASS  prose scan-back: docs/superpowers/ is a frozen store and skipped
  PASS  prose scan-back: docs/investigations/ is a frozen store and skipped
  PASS  prose lists: the British synchronize stem is published; a root README carrying it is red
  PASS  prose lists: synchronism, synchronisms and synchronistic are allowed
  PASS  prose scan-back: restating the kit's own folder in skipDirs does not un-scan the root README
  PASS  prose scan-back: a restated kit folder in skipDirs is disclosed as suppressing nothing
  PASS  prose scan-back: a restated kit folder in skipDirs, not ignored, is refused as matching nothing
  PASS  prose scan-back: a restated kit folder in skipDirs that .pkgmeta ignores passes the refusal
  PASS  prose lists: PUBLISHED_BRITISH == #BRITISH == 92 and PUBLISHED_ALLOWED == #ALLOWED == 33
  PASS  runner perf: a performance-§12 register row records skip reason (2), in the manifest and RESULTS.md
  PASS  runner perf: a library's root CLAUDE.md register is read when there is no docs/ARCHITECTURE.md
  PASS  runner perf: with no performance-§12 row the skip stays reason (1)
  PASS  runner perf: a row that disclaims the exemption is not the exemption
  PASS  runner perf: an unparseable register exits 2 before any bundle is written
  PASS  runner perf: KA0S_PERF_EXEMPT=1 counts only where no register exists
  PASS  runner complexity: empty watch-list tables print their header rows, never 'None.'
  PASS  eol: every tracked file carries the terminator .gitattributes declares for it
  PASS  eol: .gitattributes is line-endings-§5's canonical body for this repo kind
  PASS  layoutcap: every authored file over the 1500-line cap is named in the census
  PASS  layoutcap: no census row outlives the breach it records
  PASS  layoutcap: every over-cap census row carries one of layout-§1's three terminal states
  PASS  layoutcap: the census and the exempt set agree about which paths were exempted
  PASS  layoutcap: an empty census is written as a result rather than left standing empty
  PASS  layoutcap self-test: the parser reads the census nested under the register, and stops there
  PASS  layoutcap self-test: a census outside its register, or at the wrong level, is not read
  PASS  layoutcap self-test: an over-cap file missing from the census is reported, and an exempt one is not
  PASS  layoutcap self-test: a census row that outlives its breach is reported
  PASS  layoutcap self-test: an over-cap row that names no terminal state is reported
  PASS  layoutcap self-test: the census and the exempt set are held to naming the same paths
  PASS  layoutcap self-test: a census that states nothing is told apart from one that states none
  PASS  layoutcap self-test: the exempt set takes folders as well as paths

1676 passed, 0 failed, 1 skipped, 1677 total
